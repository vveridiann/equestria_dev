BAN_mercs_and_blackmarket = {
	# Disabled for AI due to being a suspected cause for CTDs
	BAN_hire_merc_1 = {
		icon = rifles
		available = {
			hidden_trigger = {
				check_variable  = {
					var = loot
					value = 1
					compare = greater_than
				}
			}
		}
		custom_cost_trigger = {
			check_variable  = {
				var = loot
				value = 1
				compare = greater_than
			}
		}
		custom_cost_text = decision_cost_2_loot
		visible = {
			has_completed_focus = BAN_black_market_mercs
			NOT = { has_country_flag = BAN_merc_1_hired }
		}

		ai_will_do = {
			factor = 0
			modifier = {
				factor = 0
				has_war = no
			}
			modifier = {
				factor = 80
				enemies_strength_ratio > 0.6
				has_war = yes
			}
		}
		cost = 25
		days_remove = 60
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BAN_hire_merc_1"
			subtract_from_variable = {
				var = loot
				value = 2
			}
			custom_effect_tooltip = BAN_merc_1_name
			hidden_effect = { 
				load_oob = "BAN_merc_1"
				set_country_flag = BAN_merc_1_hired
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_hire_merc_1"
			effect_tooltip = {
				delete_unit_template_and_units = { division_template = "Blackhollow Highwaygriffs" }
			}
			clr_country_flag = BAN_merc_1_hired
			hidden_effect = { country_event = bandit.1000 }
		}
	}

	BAN_hire_merc_2 = {
		icon = rifles
		available = {
			hidden_trigger = {
				check_variable  = {
					var = loot
					value = 3
					compare = greater_than
				}
			}
		}
		custom_cost_trigger = {
			check_variable  = {
				var = loot
				value = 3
				compare = greater_than
			}
		}
		custom_cost_text = decision_cost_4_loot
		visible = {
			has_completed_focus = BAN_black_market_mercs
			NOT = { has_country_flag = BAN_merc_2_hired }
		}

		ai_will_do = {
			factor = 0
			modifier = {
				factor = 0
				has_war = no
			}
			modifier = {
				factor = 60
				enemies_strength_ratio > 0.6
				has_war = yes
			}
		}
		cost = 25
		days_remove = 100
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BAN_hire_merc_2"
			subtract_from_variable = {
				var = loot
				value = 4
			}
			custom_effect_tooltip = BAN_merc_2_name
			hidden_effect = { 
				load_oob = "BAN_merc_2"
				set_country_flag = BAN_merc_2_hired
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_hire_merc_2"
			effect_tooltip = {
				delete_unit_template_and_units = { division_template = "Sicameon Privateers" }
			}
			clr_country_flag = BAN_merc_2_hired
			hidden_effect = { country_event = bandit.1001 }
		}
	}

	BAN_hire_merc_3 = {
		icon = rifles
		available = {
			hidden_trigger = {
				check_variable  = {
					var = loot
					value = 5
					compare = greater_than
				}
			}
		}
		custom_cost_trigger = {
			check_variable  = {
				var = loot
				value = 5
				compare = greater_than
			}
		}
		custom_cost_text = decision_cost_6_loot
		visible = {
			has_completed_focus = BAN_black_market_mercs
			NOT = { has_country_flag = BAN_merc_3_hired }
		}

		ai_will_do = {
			factor = 0
			modifier = {
				factor = 0
				has_war = no
			}
			modifier = {
				factor = 40
				enemies_strength_ratio > 0.6
				has_war = yes
			}
		}
		cost = 25
		days_remove = 120
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BAN_hire_merc_3"
			subtract_from_variable = {
				var = loot
				value = 6
			}
			custom_effect_tooltip = BAN_merc_3_name
			hidden_effect = { 
				load_oob = "BAN_merc_3"
				set_country_flag = BAN_merc_3_hired
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_hire_merc_3"
			effect_tooltip = {
				delete_unit_template_and_units = { division_template = "Lushian Hussars" }
			}
			clr_country_flag = BAN_merc_3_hired
			hidden_effect = { country_event = bandit.1002 }
		}
	}

	BAN_hire_merc_4 = {
		icon = rifles
		available = {
			hidden_trigger = {
				check_variable  = {
					var = loot
					value = 7
					compare = greater_than
				}
			}
		}
		custom_cost_trigger = {
			check_variable  = {
				var = loot
				value = 7
				compare = greater_than
			}
		}
		custom_cost_text = decision_cost_8_loot
		visible = {
			has_completed_focus = BAN_black_market_mercs
			NOT = { 
				has_country_flag = BAN_merc_4_hired
				has_completed_focus = BAN_integrate_sons_of_arcturius
			}
		}

		ai_will_do = {
			factor = 0
			modifier = {
				factor = 0
				has_war = no
			}
			modifier = {
				factor = 20
				enemies_strength_ratio > 0.6
				has_war = yes
			}
		}
		cost = 25
		days_remove = 140
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BAN_hire_merc_4"
			subtract_from_variable = {
				var = loot
				value = 8
			}
			custom_effect_tooltip = BAN_merc_4_name
			hidden_effect = { 
				fix_weapon_techs = yes
				if = {
					limit = {
						has_tech = tech_knights_magical
					}
					add_equipment_to_stockpile = {
						type = magical_equipment
						amount = 900
					}
				}
				else = {
					add_equipment_to_stockpile = {
						type = infantry_equipment
						amount = 900
					}
				}
				load_oob = "BAN_merc_4"
				set_country_flag = BAN_merc_4_hired
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_hire_merc_4"
			effect_tooltip = {
				delete_unit_template_and_units = { division_template = "Sons of Arcturius" }
			}
			clr_country_flag = BAN_merc_4_hired
			hidden_effect = { 
				if = {
					limit = {
						has_tech = tech_knights_magical
					}
					add_equipment_to_stockpile = {
						type = magical_equipment
						amount = -900
					}
				}
				else = {
					add_equipment_to_stockpile = {
						type = infantry_equipment
						amount = -900
					}
				}
				country_event = bandit.1003 
			}
		}
	}

	BAN_new_bandits = {
		icon = griffon_bandits
		allowed = {
			original_tag = BAN
		}
		visible = {
			has_completed_focus = BAN_black_market_deals
			NOT = {
				has_government = democratic
				has_government = communism
			}
		}
		available = {
			hidden_trigger = {
				check_variable  = {
					var = loot
					value = 1
					compare = greater_than
				}
			}
		}
		custom_cost_trigger = {
			check_variable  = {
				var = loot
				value = 1
				compare = greater_than
			}
		}
		custom_cost_text = decision_cost_2_loot

		days_re_enable = 120

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 20
				has_manpower < 20000
				check_variable = {
					var = loot
					value > 3
				}
			}

			modifier = {
				factor = 100
				has_war = yes
			}
		}
		cost = 25
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BAN_new_bandits"
			subtract_from_variable = {
				var = loot
				value = 2
			}
			add_manpower = 10000
		}
	}
	BAN_definitely_not_slaves = {
		allowed = {
			original_tag = BAN
		}
		icon = griffon_bandits
		visible = {
			has_completed_focus = BAN_black_market_expansion
			NOT = {
				has_government = democratic
				has_government = communism
			}
		}
		available = {
			hidden_trigger = {
				check_variable  = {
					var = loot
					value = 3
					compare = greater_than
				}
			}
			NOT = {
				has_idea = BAN_foreign_laborers
			}
		}
		custom_cost_trigger = {
			check_variable  = {
				var = loot
				value = 3
				compare = greater_than
			}
		}
		custom_cost_text = decision_cost_4_loot

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 60
				num_of_factories > 4
			}

			modifier = {
				factor = 40
				num_of_factories < 10
			}

			modifier = {
				factor = 20
				num_of_factories > 10
			}
		}
		cost = 25
		days_remove = 10
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BAN_definitely_not_slaves"
			subtract_from_variable = {
				var = loot
				value = 4
			}

			add_timed_idea = { idea = BAN_foreign_laborers days = 180 }
			random_owned_controlled_state = {
				add_manpower = 10000
			}
		}
	}
	BAN_weapons = {
		icon = griffon_bandits
		allowed = {
			original_tag = BAN
		}
		visible = {
			has_completed_focus = BAN_black_market_deals
			NOT = {
				has_government = democratic
				has_government = communism
			}
		}
		available = {
			hidden_trigger = {
				check_variable  = {
					var = loot
					value = 4
					compare = greater_than
				}
			}
		}
		custom_cost_trigger = {
			check_variable  = {
				var = loot
				value = 4
				compare = greater_than
			}
		}
		custom_cost_text = decision_cost_5_loot
		cost = 25
		days_remove = 10
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BAN_weapons"
			subtract_from_variable = {
				var = loot
				value = 5
			}

			if = {
				limit = {
					has_tech = advanced_infantry_weapons
				}
				add_equipment_to_stockpile = {
					type = infantry_equipment_3
					amount = 1000
					producer = BAN
				}
			}
			else_if = {
				limit = {
					has_tech = improved_infantry_weapons
				}
				add_equipment_to_stockpile = {
					type = infantry_equipment_2
					amount = 1000
					producer = BAN
				}
			}
			else_if = {
				limit = {
					has_tech = infantry_weapons1
				}
				add_equipment_to_stockpile = {
					type = infantry_equipment_1
					amount = 1000
					producer = BAN
				}
			}
			else_if = {
				limit = {
					has_tech = infantry_weapons
				}
				add_equipment_to_stockpile = {
					type = infantry_equipment_0
					amount = 1000
					producer = BAN
				}
			}
			else_if = {
				limit = {
					has_tech = melee_weapons1
				}

				add_equipment_to_stockpile = {
					type = melee_equipment_1
					amount = 1000
					producer = BAN
				}
			}

			else_if = {
				limit = {
					has_tech = melee_weapons
				}

				add_equipment_to_stockpile = {
					type = melee_equipment_0
					amount = 1000
					producer = BAN
				}
			}

		}
		ai_will_do = {
			factor = 10
			modifier = {
				factor = 40
				stockpile_ratio = {
					archetype = infantry_equipment
					ratio < 0.6
				}
			}

			modifier = {
				factor = 60
				stockpile_ratio = {
					archetype = infantry_equipment
					ratio < 0.6
				}
				has_war = yes
			}
		}
	}

	BAN_blueprints = {
		icon = griffon_bandits
		allowed = {
			original_tag = BAN
		}
		visible = {
			has_completed_focus = BAN_black_market_deals
			NOT = {
				has_government = democratic
				has_government = communism
			}
		}
		available = {
			hidden_trigger = {
				check_variable  = {
					var = loot
					value = 7
					compare = greater_than
				}
			}
		}
		custom_cost_trigger = {
			check_variable  = {
				var = loot
				value = 7
				compare = greater_than
			}
		}
		custom_cost_text = decision_cost_8_loot
		days_remove = 10
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BAN_blueprints"
			subtract_from_variable = {
				var = loot
				value = 8
			}

			country_event = {
				id = bandit.4
			}
		}
		ai_will_do = {
			factor = 10
		}
		cost = 25
	}

	BAN_stealing_scientists = {
		icon = griffon_bandits
		allowed = {
			original_tag = BAN
		}
		visible = {
			has_completed_focus = BAN_black_market_expansion
			NOT = {
				has_government = democratic
				has_government = communism
			}
		}
		available = {
			hidden_trigger = {
				check_variable  = {
					var = loot
					value = 7
					compare = greater_than
				}
			}
			NOT = {
				has_idea = BAN_stolen_scientists
			}
		}
		custom_cost_trigger = {
			check_variable  = {
				var = loot
				value = 7
				compare = greater_than
			}
		}
		custom_cost_text = decision_cost_8_loot
		days_remove = 10
		cost = 25
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BAN_stealing_scientists"
			subtract_from_variable = {
				var = loot
				value = 8
			}
			add_timed_idea = { idea = BAN_stolen_scientists days = 200 }

		}
		ai_will_do = {
			factor = 40
		}
	}
}

BAN_tributaries = {
	BAN_demand_tribute = {
		icon = griffon_bandits
		visible = {
			has_completed_focus = BAN_tributary_kingdoms
			has_government = fascism
			FROM = { exists = yes }
			NOT = { has_war_with = FROM }
		}
		target_root_trigger = {
			has_completed_focus = BAN_tributary_kingdoms
			has_government = fascism
		}
		target_trigger = { ## Lets the decision target multiple tags
			FROM = {
				has_country_flag = bandit_tributary
			}
		}
		ai_will_do = {
			factor = 20
			modifier = {
				factor = 100
				check_variable = {
					loot < 10
				}
			}
		}

		fire_only_once = no
		cost = 25
		days_re_enable = 120

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BAN_demand_tribute target: [From.GetName]"
			FROM = {
				country_event = { id = bandit.2 }
			}
		}
	}


	BAN_force_tributary = {
		icon = griffon
		visible = {
			has_completed_focus = BAN_tributary_kingdoms
			has_government = fascism
			FROM = {
				exists = yes
				is_major = no
				NOT = { 
					is_subject_of = ROOT 
					is_in_faction_with = ROOT
					has_idea = detached_country
					has_country_flag = bandit_tributary
					tag = event_target:griffonian_empire
					is_subject_of = event_target:griffonian_empire
					is_in_faction_with = event_target:griffonian_empire
				}
			}
			NOT = { 
				has_war_with = FROM
				is_subject_of = FROM 
			}
		}
		target_root_trigger = {
			has_completed_focus = BAN_tributary_kingdoms
			has_government = fascism
		}
		target_array = neighbors
		target_trigger = { ## Lets the decision target multiple tags
			FROM = {
				is_neighbor_of = ROOT
				NOT = {
					has_country_flag = bandit_tributary
				}	
			}
		}
		ai_will_do = {
			factor = 10
			modifier = {
				factor = 100
				strength_ratio = {
					tag = FROM
					ratio < 0.7
				}
			}
			modifier = {
				factor = 0
				strength_ratio = {
					tag = FROM
					ratio > 1
				}
			}
		}
		cost = 100
		fire_only_once = no
		days_re_enable = 120
		days_remove = 5

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_force_tributary target: [From.GetName]"
			FROM = {
				country_event = { id = bandit.6 }
			}
		}
	}
}

BAN_society_development_cat = {
	BAN_raise_society = {
		available = {

		}
		visible = {
			NOT = { has_idea = modern_society }
			OR = {
				has_completed_focus = BAN_lawless_no_more
				has_completed_focus = BAN_control_restored
				has_completed_focus = BAN_back_in_power
			}
		}

		cost = 100
		fire_only_once = no
		days_re_enable = 70
		days_remove = 70

		ai_will_do = {
			factor = 100
		}

		modifier = {
			political_power_gain = -0.15
			stability_factor = -0.10
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_raise_society"
			increase_society_development_gradual = yes
		}
	}
}


BAN_civil_war = {
	BAN_deadline = {
		icon = rifles
		activation = {
			NOT = { has_country_flag = BAN_stop_resistance_shit_flag }
			OR = {
				has_completed_focus = BAN_noble_spirit
				has_completed_focus = BAN_revolutionary_spirit
			}
		}
		days_mission_timeout = 500
		available = {
			NOT = {
				446 = { has_state_flag = BAN_actual_controller_bandits }
				489 = { has_state_flag = BAN_actual_controller_bandits }
				490 = { has_state_flag = BAN_actual_controller_bandits }
				532 = { has_state_flag = BAN_actual_controller_bandits }
				606 = { has_state_flag = BAN_actual_controller_bandits }
			}
			OR = {
				has_completed_focus = BAN_oppose_communism
				has_completed_focus = BAN_united_resistance_cyrille
				has_completed_focus = BAN_united_resistance_reeve
				has_completed_focus = BAN_no_compromise
			}
			OR = {
				has_completed_focus = BAN_allies_in_temples
				has_completed_focus = BAN_common_goals_with_clergy
			}
		}
		cost = 0
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			if = {
				limit = {
					has_completed_focus = BAN_noble_spirit
				}
				set_country_flag = BAN_was_cyrille
			}
			if = {
				limit = {
					has_completed_focus = BAN_revolutionary_spirit
				}
				set_country_flag = BAN_was_reeve
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_deadline"
			set_country_flag = BAN_stop_resistance_shit_flag
			if = {
				limit = {
					has_completed_focus = BAN_oppose_communism
					NOT = {
						has_completed_focus = BAN_bring_communes_to_heel
					}
				}
				country_event = { id = bandit.51 }
				else_if = {
					limit = {
						has_completed_focus = BAN_bring_communes_to_heel
					}
					country_event = { id = bandit.47 }
					else_if = {
						limit = {
							OR = {
								has_completed_focus = BAN_united_resistance_cyrille
								has_completed_focus = BAN_united_resistance_reeve
							}
						}
						country_event = { id = bandit.48 }
						else_if = {
							limit = {
								has_completed_focus = BAN_no_compromise
								NOT = {
									has_completed_focus = BAN_contain_nobles
								}
							}
							country_event = { id = bandit.49 }
							else_if = {
								limit = {
									has_completed_focus = BAN_contain_nobles
								}
								country_event = { id = bandit.50 }
							}
						}
					}
				}
			}
		}
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_deadline"
			set_country_flag = BAN_stop_resistance_shit_flag
			complete_focus = BAN_gather_tribute
			# needs further work to make functional
		}
	}
	BAN_can_win_naniwich_test = {
		icon = rifles
		visible = {
			original_tag = BAN
		}
		available = {
			489 = {
				BAN_battle_win = yes
			}
		}
		cost = 0
	}
	BAN_stop_assassination = {
		icon = griffon
		visible = {
			OR = {
				AND = {
					has_active_mission = BAN_auto_reeve_kill_pinfeather
					has_country_flag = BAN_cyrille_pinfeather
				}
				AND = {
					has_active_mission = BAN_auto_cyrille_kill_pinfeather
					has_country_flag = BAN_reeve_pinfeather
				}
				has_active_mission = BAN_auto_reeve_kill_blackbeak
				has_active_mission = BAN_auto_reeve_kill_sir
				has_active_mission = BAN_auto_cyrille_kill_tercel
				has_active_mission = BAN_auto_cyrille_kill_horndrogiannis
			}
		}
		available = {
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		cost = 0
		days_remove = 15
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_stop_assassination"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_stop_assassination_flag
			clr_country_flag = BAN_cyrille_target_pinfeather
			clr_country_flag = BAN_reeve_target_pinfeather
			clr_country_flag = BAN_cyrille_target_tercel
			clr_country_flag = BAN_reeve_target_blackbeak
			clr_country_flag = BAN_cyrille_target_horndrogiannis
			clr_country_flag = BAN_reeve_target_sir
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_stop_assassination"
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			clr_country_flag = BAN_stop_assassination
		}
	}
	BAN_auto_reeve_start = { # Starts all the shit for Reeve if you go for Cyrille
		icon = griffon
		activation = { has_country_flag = BAN_reeve_start }
		available = { always = no }
		days_mission_timeout = 10
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_start"
			custom_effect_tooltip = BAN_add_3_commune_control_creeper_tt
			hidden_effect = {
				set_country_flag = BAN_auto_reeve_increase_446_flag
				446 = {
					if = {
						limit = {
							check_variable = { BAN_bandit_control > 2 }
						}
						add_to_variable = {
							BAN_commune_control = 3
						}
						add_to_variable = {
							BAN_bandit_control = -3
						}
						else_if = {
							limit = {
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_commune_control = 1
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.43
				}
				clr_country_flag = BAN_auto_reeve_increase_446_flag
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_reeve_kill_pinfeather = {
		icon = griffon
		activation = { has_country_flag = BAN_reeve_target_pinfeather }
		available = {
			has_country_flag = BAN_stop_assassination_flag
		}
		cost = 0
		days_remove = 10
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_kill_pinfeather"
			remove_unit_leader = 69001
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_pinfeather_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_dominik_controls_state
						}
						clr_state_flag = BAN_dominik_controls_state
						set_state_flag = BAN_blackrock_controls_state
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 4 }
							}
							add_to_variable = {
								BAN_bandit_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 3 }
								}
								add_to_variable = {
									BAN_bandit_control = -4
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 2 }
									}
									add_to_variable = {
										BAN_bandit_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control > 1 }
										}
										add_to_variable = {
											BAN_bandit_control = -2
										}
										add_to_variable = {
											BAN_noble_control = 1
										}
										add_to_variable = {
											BAN_commune_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_bandit_control > 0 }
											}
											add_to_variable = {
												BAN_bandit_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_reeve_kill_bronsfeather = {
		icon = griffon
		activation = { has_country_flag = BAN_reeve_target_bronsfeather }
		available = {
			always = no
		}
		cost = 0
		days_remove = 10
		days_mission_timeout = 30
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_auto_reeve_kill_bronsfeather"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_auto_reeve_kill_bronsfeather"
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
		}
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_kill_bronsfeather"
			remove_unit_leader = 69003
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_bronsfeather_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_kajetan_controls_state
						}
						clr_state_flag = BAN_kajetan_controls_state
						set_state_flag = BAN_blackrock_controls_state
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 4 }
							}
							add_to_variable = {
								BAN_bandit_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 3 }
								}
								add_to_variable = {
									BAN_bandit_control = -4
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 2 }
									}
									add_to_variable = {
										BAN_bandit_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control > 1 }
										}
										add_to_variable = {
											BAN_bandit_control = -2
										}
										add_to_variable = {
											BAN_noble_control = 1
										}
										add_to_variable = {
											BAN_commune_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_bandit_control > 0 }
											}
											add_to_variable = {
												BAN_bandit_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_reeve_kill_hiperion = {
		icon = griffon
		activation = { has_country_flag = BAN_reeve_target_hiperion }
		available = {
			always = no
		}
		cost = 0
		days_remove = 10
		days_mission_timeout = 30
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_auto_reeve_kill_hiperion"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_auto_reeve_kill_hiperion"
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
		}
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_kill_hiperion"
			remove_unit_leader = 69002
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_hiperion_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_ulrich_controls_state
						}
						clr_state_flag = BAN_ulrich_controls_state
						set_state_flag = BAN_blackrock_controls_state
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 4 }
							}
							add_to_variable = {
								BAN_bandit_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 3 }
								}
								add_to_variable = {
									BAN_bandit_control = -4
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 2 }
									}
									add_to_variable = {
										BAN_bandit_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control > 1 }
										}
										add_to_variable = {
											BAN_bandit_control = -2
										}
										add_to_variable = {
											BAN_noble_control = 1
										}
										add_to_variable = {
											BAN_commune_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_bandit_control > 0 }
											}
											add_to_variable = {
												BAN_bandit_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_reeve_kill_blackbeak = {
		icon = griffon
		activation = { has_country_flag = BAN_reeve_target_blackbeak }
		available = {
			has_country_flag = BAN_stop_assassination_flag
		}
		cost = 0
		days_remove = 10
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_kill_blackbeak"
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_blackbeak_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_silas_controls_state
						}
						clr_state_flag = BAN_silas_controls_state
						set_state_flag = BAN_cyrille_controls_state
						if = {
							limit = {
								check_variable = { BAN_noble_control > 4 }
							}
							add_to_variable = {
								BAN_noble_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_bandit_control = 2
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_noble_control > 3 }
								}
								add_to_variable = {
									BAN_noble_control = -4
								}
								add_to_variable = {
									BAN_bandit_control = 2
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_noble_control > 2 }
									}
									add_to_variable = {
										BAN_noble_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = 1
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_noble_control > 1 }
										}
										add_to_variable = {
											BAN_noble_control = -2
										}
										add_to_variable = {
											BAN_bandit_control = 1
										}
										add_to_variable = {
											BAN_commune_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_noble_control > 0 }
											}
											add_to_variable = {
												BAN_noble_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_reeve_kill_sir = {
		icon = griffon
		activation = { has_country_flag = BAN_reeve_target_sir }
		available = {
			has_country_flag = BAN_stop_assassination_flag
		}
		cost = 0
		days_remove = 10
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_kill_sir"
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_sir_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_sir_controls_state
						}
						clr_state_flag = BAN_sir_controls_state
						set_state_flag = BAN_cyrille_controls_state
						if = {
							limit = {
								check_variable = { BAN_noble_control > 4 }
							}
							add_to_variable = {
								BAN_noble_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_bandit_control = 2
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_noble_control > 3 }
								}
								add_to_variable = {
									BAN_noble_control = -4
								}
								add_to_variable = {
									BAN_bandit_control = 2
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_noble_control > 2 }
									}
									add_to_variable = {
										BAN_noble_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = 1
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_noble_control > 1 }
										}
										add_to_variable = {
											BAN_noble_control = -2
										}
										add_to_variable = {
											BAN_bandit_control = 1
										}
										add_to_variable = {
											BAN_commune_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_noble_control > 0 }
											}
											add_to_variable = {
												BAN_noble_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_reeve_increase_446 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_reeve_increase_446_flag }
		available = { always = no }
		days_mission_timeout = 20
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_increase_446"
			custom_effect_tooltip = BAN_add_3_commune_control_creeper_tt
			hidden_effect = {
				set_country_flag = BAN_auto_reeve_increase_446_flag
				446 = {
					if = {
						limit = {
							check_variable = { BAN_bandit_control > 2 }
						}
						add_to_variable = {
							BAN_commune_control = 3
						}
						add_to_variable = {
							BAN_bandit_control = -3
						}
						else_if = {
							limit = {
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_commune_control = 1
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.43
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_reeve_attack_446 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_reeve_attack_446_flag }
		available = { always = no }
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_attack_446"
			custom_effect_tooltip = BAN_reeve_attack_446
			hidden_effect = {
				set_country_flag = BAN_auto_reeve_attack_446_flag
				if = {
					limit = {
						446 = { has_state_flag = BAN_actual_controller_bandits }
					}
					subtract_from_variable = {
						var = BAN_bandit_divisions
						value = 446.BAN_bandit_garrison
					}
					subtract_from_variable = {
						var = BAN_special_bandit_divisions
						value = 446.BAN_special_bandit_garrison
					}
					446 = {
						set_variable = {
							BAN_bandit_garrison = 0
						}
						set_variable = {
							BAN_special_bandit_garrison = 0
						}
						set_variable = {
							BAN_commune_garrison = 1
						}
						clr_state_flag = BAN_actual_controller_bandits
						clr_state_flag = BAN_blackrock_controls_state
						clr_state_flag = BAN_ulrich_controls_state
						clr_state_flag = BAN_dominik_controls_state
						clr_state_flag = BAN_kajetan_controls_state
						set_state_flag = BAN_actual_controller_communes
						set_state_flag = BAN_reeve_controls_state
					}
					else_if = { # will need an event for this at some point
						limit = {
							446 = { has_state_flag = BAN_actual_controller_nobles }
						}
						if = {
							limit = {
								446 = {
									OR = {
										AND = {
											check_variable = { BAN_noble_garrison = 0 }
											check_variable = { BAN_special_noble_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_noble_garrison = 1 }
											check_variable = { BAN_special_noble_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_noble_garrison = 0 }
											check_variable = { BAN_special_noble_garrison = 1 }
										}
									}
								}
							}
							subtract_from_variable = {
								var = BAN_noble_divisions
								value = 446.BAN_noble_garrison
							}
							subtract_from_variable = {
								var = BAN_special_noble_divisions
								value = 446.BAN_special_noble_divisions
							}
							446 = {
								set_variable = {
									BAN_noble_garrison = 0
								}
								set_variable = {
									BAN_special_noble_garrison = 0
								}
								set_variable = {
									BAN_commune_garrison = 1
								}
								clr_state_flag = BAN_actual_controller_nobles
								clr_state_flag = BAN_cyrille_controls_state
								clr_state_flag = BAN_silas_controls_state
								clr_state_flag = BAN_sir_controls_state
								set_state_flag = BAN_actual_controller_communes
								set_state_flag = BAN_reeve_controls_state
							}
							else = {
								add_to_variable = {
									BAN_commune_divisions = -2
								}
								add_to_variable = {
									BAN_noble_divisions = -1
								}
								446 = {
									add_to_variable = {
										BAN_noble_garrison = -1
									}
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.43
				}
				update_BAN_variables = yes
				if = {
					limit = {
						ROOT = { check_variable = { BAN_available_commune_divisions < 3 } }
					}
					activate_decision = BAN_auto_reeve_recruit_division
					set_country_flag = BAN_auto_reeve_recruiting
				}
			}
		}
	}
	BAN_auto_reeve_increase_606 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_reeve_increase_606_flag }
		available = { always = no }
		days_mission_timeout = 20
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_increase_606"
			custom_effect_tooltip = BAN_add_3_commune_control_banditplace_tt
			hidden_effect = {
				set_country_flag = BAN_auto_reeve_increase_606_flag
				606 = {
					if = {
						limit = {
							check_variable = { BAN_bandit_control > 2 }
						}
						add_to_variable = {
							BAN_commune_control = 3
						}
						add_to_variable = {
							BAN_bandit_control = -3
						}
						else_if = {
							limit = {
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_commune_control = 1
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.43
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_reeve_attack_606 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_reeve_attack_606_flag }
		available = { always = no }
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_attack_606"
			custom_effect_tooltip = BAN_reeve_attack_606
			hidden_effect = {
				set_country_flag = BAN_auto_reeve_attack_606_flag
				if = {
					limit = {
						606 = { has_state_flag = BAN_actual_controller_bandits }
					}
					subtract_from_variable = {
						var = BAN_bandit_divisions
						value = 606.BAN_bandit_garrison
					}
					subtract_from_variable = {
						var = BAN_special_bandit_divisions
						value = 606.BAN_special_bandit_garrison
					}
					606 = {
						set_variable = {
							BAN_bandit_garrison = 0
						}
						set_variable = {
							BAN_special_bandit_garrison = 0
						}
						set_variable = {
							BAN_commune_garrison = 1
						}
						clr_state_flag = BAN_actual_controller_bandits
						clr_state_flag = BAN_blackrock_controls_state
						clr_state_flag = BAN_ulrich_controls_state
						clr_state_flag = BAN_dominik_controls_state
						clr_state_flag = BAN_kajetan_controls_state
						set_state_flag = BAN_actual_controller_communes
						set_state_flag = BAN_reeve_controls_state
					}
					else_if = { # will need an event for this at some point
						limit = {
							606 = { has_state_flag = BAN_actual_controller_nobles }
						}
						if = {
							limit = {
								606 = {
									OR = {
										AND = {
											check_variable = { BAN_noble_garrison = 0 }
											check_variable = { BAN_special_noble_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_noble_garrison = 1 }
											check_variable = { BAN_special_noble_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_noble_garrison = 0 }
											check_variable = { BAN_special_noble_garrison = 1 }
										}
									}
								}
							}
							subtract_from_variable = {
								var = BAN_noble_divisions
								value = 606.BAN_noble_garrison
							}
							subtract_from_variable = {
								var = BAN_special_noble_divisions
								value = 606.BAN_special_noble_divisions
							}
							606 = {
								set_variable = {
									BAN_noble_garrison = 0
								}
								set_variable = {
									BAN_special_noble_garrison = 0
								}
								set_variable = {
									BAN_commune_garrison = 1
								}
								clr_state_flag = BAN_actual_controller_nobles
								clr_state_flag = BAN_cyrille_controls_state
								clr_state_flag = BAN_silas_controls_state
								clr_state_flag = BAN_sir_controls_state
								set_state_flag = BAN_actual_controller_communes
								set_state_flag = BAN_reeve_controls_state
							}
							else = {
								add_to_variable = {
									BAN_commune_divisions = -2
								}
								add_to_variable = {
									BAN_noble_divisions = -1
								}
								606 = {
									add_to_variable = {
										BAN_noble_garrison = -1
									}
								}
								if = {
									limit = {
										606 = { 
											OR = { 
												has_state_flag = BAN_actual_controller_bandits 
												AND = { 
													ROOT = { has_completed_focus = BAN_oppose_communism } has_state_flag = BAN_actual_controller_nobles
												}
											}
										}
									}
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.43
				}

				update_BAN_variables = yes
				if = {
					limit = {
						ROOT = { check_variable = { BAN_available_commune_divisions < 3 } }
					}
					activate_decision = BAN_auto_reeve_recruit_division
					set_country_flag = BAN_auto_reeve_recruiting
				}
			}
		}
	}
	BAN_auto_reeve_increase_490 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_reeve_increase_490_flag }
		available = { always = no }
		days_mission_timeout = 20
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_increase_490"
			custom_effect_tooltip = BAN_add_3_commune_control_blackhollow_tt
			hidden_effect = {
				set_country_flag = BAN_auto_reeve_increase_490_flag
				490 = {
					if = {
						limit = {
							check_variable = { BAN_bandit_control > 2 }
						}
						add_to_variable = {
							BAN_commune_control = 3
						}
						add_to_variable = {
							BAN_bandit_control = -3
						}
						else_if = {
							limit = {
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_commune_control = 1
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.43
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_reeve_attack_490 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_reeve_attack_490_flag }
		available = { always = no }
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_attack_490"
			custom_effect_tooltip = BAN_reeve_attack_490
			hidden_effect = {
				set_country_flag = BAN_auto_reeve_attack_490_flag
				if = {
					limit = {
						490 = { has_state_flag = BAN_actual_controller_bandits }
					}
					subtract_from_variable = {
						var = BAN_bandit_divisions
						value = 490.BAN_bandit_garrison
					}
					subtract_from_variable = {
						var = BAN_special_bandit_divisions
						value = 490.BAN_special_bandit_garrison
					}
					490 = {
						set_variable = {
							BAN_bandit_garrison = 0
						}
						set_variable = {
							BAN_special_bandit_garrison = 0
						}
						set_variable = {
							BAN_commune_garrison = 1
						}
						clr_state_flag = BAN_actual_controller_bandits
						clr_state_flag = BAN_blackrock_controls_state
						clr_state_flag = BAN_ulrich_controls_state
						clr_state_flag = BAN_dominik_controls_state
						clr_state_flag = BAN_kajetan_controls_state
						set_state_flag = BAN_actual_controller_communes
						set_state_flag = BAN_reeve_controls_state
					}
					else_if = { # will need an event for this at some point
						limit = {
							490 = { has_state_flag = BAN_actual_controller_nobles }
						}
						if = {
							limit = {
								490 = {
									OR = {
										AND = {
											check_variable = { BAN_noble_garrison = 0 }
											check_variable = { BAN_special_noble_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_noble_garrison = 1 }
											check_variable = { BAN_special_noble_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_noble_garrison = 0 }
											check_variable = { BAN_special_noble_garrison = 1 }
										}
									}
								}
							}
							subtract_from_variable = {
								var = BAN_noble_divisions
								value = 490.BAN_noble_garrison
							}
							subtract_from_variable = {
								var = BAN_special_noble_divisions
								value = 490.BAN_special_noble_divisions
							}
							490 = {
								set_variable = {
									BAN_noble_garrison = 0
								}
								set_variable = {
									BAN_special_noble_garrison = 0
								}
								set_variable = {
									BAN_commune_garrison = 1
								}
								clr_state_flag = BAN_actual_controller_nobles
								clr_state_flag = BAN_cyrille_controls_state
								clr_state_flag = BAN_silas_controls_state
								clr_state_flag = BAN_sir_controls_state
								set_state_flag = BAN_actual_controller_communes
								set_state_flag = BAN_reeve_controls_state
							}
							else = {
								add_to_variable = {
									BAN_commune_divisions = -2
								}
								add_to_variable = {
									BAN_noble_divisions = -1
								}
								490 = {
									add_to_variable = {
										BAN_noble_garrison = -1
									}
								}
								if = {
									limit = {
										490 = { 
											OR = { 
												has_state_flag = BAN_actual_controller_bandits 
												AND = { 
													ROOT = { has_completed_focus = BAN_oppose_communism } has_state_flag = BAN_actual_controller_nobles
												}
											}
										}
									}
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.43
				}
				update_BAN_variables = yes
				if = {
					limit = {
						ROOT = { check_variable = { BAN_available_commune_divisions < 3 } }
					}
					activate_decision = BAN_auto_reeve_recruit_division
					set_country_flag = BAN_auto_reeve_recruiting
				}
			}
		}
	}
	BAN_auto_reeve_increase_489 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_reeve_increase_489_flag }
		available = { always = no }
		days_mission_timeout = 20
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_increase_489"
			custom_effect_tooltip = BAN_add_3_commune_control_naniwich_tt
			hidden_effect = {
				set_country_flag = BAN_auto_reeve_increase_489_flag
				489 = {
					if = {
						limit = {
							check_variable = { BAN_bandit_control > 2 }
						}
						add_to_variable = {
							BAN_commune_control = 3
						}
						add_to_variable = {
							BAN_bandit_control = -3
						}
						else_if = {
							limit = {
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_commune_control = 1
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.43
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_reeve_attack_489 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_reeve_attack_489_flag }
		available = { always = no }
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_attack_489"
			custom_effect_tooltip = BAN_reeve_attack_489
			hidden_effect = {
				set_country_flag = BAN_auto_reeve_attack_489_flag
				if = {
					limit = {
						489 = { has_state_flag = BAN_actual_controller_bandits }
					}
					subtract_from_variable = {
						var = BAN_bandit_divisions
						value = 489.BAN_bandit_garrison
					}
					subtract_from_variable = {
						var = BAN_special_bandit_divisions
						value = 489.BAN_special_bandit_garrison
					}
					489 = {
						set_variable = {
							BAN_bandit_garrison = 0
						}
						set_variable = {
							BAN_special_bandit_garrison = 0
						}
						set_variable = {
							BAN_commune_garrison = 1
						}
						clr_state_flag = BAN_actual_controller_bandits
						clr_state_flag = BAN_blackrock_controls_state
						clr_state_flag = BAN_ulrich_controls_state
						clr_state_flag = BAN_dominik_controls_state
						clr_state_flag = BAN_kajetan_controls_state
						set_state_flag = BAN_actual_controller_communes
						set_state_flag = BAN_reeve_controls_state
					}
					else_if = { # will need an event for this at some point
						limit = {
							489 = { has_state_flag = BAN_actual_controller_nobles }
						}
						if = {
							limit = {
								489 = {
									OR = {
										AND = {
											check_variable = { BAN_noble_garrison = 0 }
											check_variable = { BAN_special_noble_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_noble_garrison = 1 }
											check_variable = { BAN_special_noble_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_noble_garrison = 0 }
											check_variable = { BAN_special_noble_garrison = 1 }
										}
									}
								}
							}
							subtract_from_variable = {
								var = BAN_noble_divisions
								value = 489.BAN_noble_garrison
							}
							subtract_from_variable = {
								var = BAN_special_noble_divisions
								value = 489.BAN_special_noble_divisions
							}
							489 = {
								set_variable = {
									BAN_noble_garrison = 0
								}
								set_variable = {
									BAN_special_noble_garrison = 0
								}
								set_variable = {
									BAN_commune_garrison = 1
								}
								clr_state_flag = BAN_actual_controller_nobles
								clr_state_flag = BAN_cyrille_controls_state
								clr_state_flag = BAN_silas_controls_state
								clr_state_flag = BAN_sir_controls_state
								set_state_flag = BAN_actual_controller_communes
								set_state_flag = BAN_reeve_controls_state
							}
							else = {
								add_to_variable = {
									BAN_commune_divisions = -2
								}
								add_to_variable = {
									BAN_noble_divisions = -1
								}
								489 = {
									add_to_variable = {
										BAN_noble_garrison = -1
									}
								}
								if = {
									limit = {
										489 = { 
											OR = { 
												has_state_flag = BAN_actual_controller_bandits 
												AND = { 
													ROOT = { has_completed_focus = BAN_oppose_communism } has_state_flag = BAN_actual_controller_nobles
												}
											}
										}
									}
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.43
				}
				update_BAN_variables = yes
				if = {
					limit = {
						ROOT = { check_variable = { BAN_available_commune_divisions < 3 } }
					}
					activate_decision = BAN_auto_reeve_recruit_division
					set_country_flag = BAN_auto_reeve_recruiting
				}
			}
		}
	}
	BAN_auto_reeve_increase_532 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_reeve_increase_532_flag }
		available = { always = no }
		days_mission_timeout = 20
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_increase_532"
			custom_effect_tooltip = BAN_add_3_commune_control_murkbeak_tt
			hidden_effect = {
				set_country_flag = BAN_auto_reeve_increase_532_flag
				532 = {
					if = {
						limit = {
							check_variable = { BAN_bandit_control > 2 }
						}
						add_to_variable = {
							BAN_commune_control = 3
						}
						add_to_variable = {
							BAN_bandit_control = -3
						}
						else_if = {
							limit = {
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_commune_control = 1
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.43
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_reeve_attack_532 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_reeve_attack_532_flag }
		available = { always = no }
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_attack_532"
			custom_effect_tooltip = BAN_reeve_attack_532
			hidden_effect = {
				set_country_flag = BAN_auto_reeve_attack_532_flag
				if = {
					limit = {
						532 = { has_state_flag = BAN_actual_controller_bandits }
					}
					subtract_from_variable = {
						var = BAN_bandit_divisions
						value = 532.BAN_bandit_garrison
					}
					subtract_from_variable = {
						var = BAN_special_bandit_divisions
						value = 532.BAN_special_bandit_garrison
					}
					532 = {
						set_variable = {
							BAN_bandit_garrison = 0
						}
						set_variable = {
							BAN_special_bandit_garrison = 0
						}
						set_variable = {
							BAN_commune_garrison = 1
						}
						clr_state_flag = BAN_actual_controller_bandits
						clr_state_flag = BAN_blackrock_controls_state
						clr_state_flag = BAN_ulrich_controls_state
						clr_state_flag = BAN_dominik_controls_state
						clr_state_flag = BAN_kajetan_controls_state
						set_state_flag = BAN_actual_controller_communes
						set_state_flag = BAN_reeve_controls_state
					}
					else_if = { # will need an event for this at some point
						limit = {
							532 = { has_state_flag = BAN_actual_controller_nobles }
						}
						if = {
							limit = {
								532 = {
									OR = {
										AND = {
											check_variable = { BAN_noble_garrison = 0 }
											check_variable = { BAN_special_noble_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_noble_garrison = 1 }
											check_variable = { BAN_special_noble_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_noble_garrison = 0 }
											check_variable = { BAN_special_noble_garrison = 1 }
										}
									}
								}
							}
							subtract_from_variable = {
								var = BAN_noble_divisions
								value = 532.BAN_noble_garrison
							}
							subtract_from_variable = {
								var = BAN_special_noble_divisions
								value = 532.BAN_special_noble_divisions
							}
							532 = {
								set_variable = {
									BAN_noble_garrison = 0
								}
								set_variable = {
									BAN_special_noble_garrison = 0
								}
								set_variable = {
									BAN_commune_garrison = 1
								}
								clr_state_flag = BAN_actual_controller_nobles
								clr_state_flag = BAN_cyrille_controls_state
								clr_state_flag = BAN_silas_controls_state
								clr_state_flag = BAN_sir_controls_state
								set_state_flag = BAN_actual_controller_communes
								set_state_flag = BAN_reeve_controls_state
							}
							else = {
								add_to_variable = {
									BAN_commune_divisions = -2
								}
								add_to_variable = {
									BAN_noble_divisions = -1
								}
								532 = {
									add_to_variable = {
										BAN_noble_garrison = -1
									}
								}
								if = {
									limit = {
										532 = { 
											OR = { 
												has_state_flag = BAN_actual_controller_bandits 
												AND = { 
													ROOT = { has_completed_focus = BAN_oppose_communism } has_state_flag = BAN_actual_controller_nobles
												}
											}
										}
									}
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.43
				}
				update_BAN_variables = yes
				if = {
					limit = {
						ROOT = { check_variable = { BAN_available_commune_divisions < 3 } }
					}
					activate_decision = BAN_auto_reeve_recruit_division
					set_country_flag = BAN_auto_reeve_recruiting
				}
			}
		}
	}
	BAN_auto_reeve_recruit_division = {
		icon = rifles
		activation = {
			has_country_flag = BAN_auto_reeve_recruiting
		}
		available = {
			always = no
		}
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_reeve_recruit_division"
			custom_effect_tooltip = BAN_recruit_commune_division
			add_to_variable = { BAN_commune_divisions = 1 }
			clr_country_flag = BAN_auto_reeve_recruiting
		}
	}
	BAN_auto_cyrille_start = { # Starts all the shit for Cyrille if you go for Reeve
		icon = griffon
		activation = { has_country_flag = BAN_cyrille_start }
		available = { always = no }
		days_mission_timeout = 10
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_start"
			custom_effect_tooltip = BAN_add_3_noble_control_naniwich_tt
			hidden_effect = {
				set_country_flag = BAN_auto_cyrille_increase_489_flag
				489 = {
					if = {
						limit = {
							check_variable = { BAN_bandit_control > 2 }
						}
						add_to_variable = {
							BAN_noble_control = 3
						}
						add_to_variable = {
							BAN_bandit_control = -3
						}
						else_if = {
							limit = {
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_noble_control = 1
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.45
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_cyrille_kill_pinfeather = {
		icon = griffon
		activation = { has_country_flag = BAN_cyrille_target_pinfeather }
		available = {
			has_country_flag = BAN_stop_assassination_flag
		}
		cost = 0
		days_remove = 10
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_kill_pinfeather"
			remove_unit_leader = 69001
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_pinfeather_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_dominik_controls_state
						}
						clr_state_flag = BAN_dominik_controls_state
						set_state_flag = BAN_blackrock_controls_state
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 4 }
							}
							add_to_variable = {
								BAN_bandit_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 3 }
								}
								add_to_variable = {
									BAN_bandit_control = -4
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 2 }
									}
									add_to_variable = {
										BAN_bandit_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control > 1 }
										}
										add_to_variable = {
											BAN_bandit_control = -2
										}
										add_to_variable = {
											BAN_noble_control = 1
										}
										add_to_variable = {
											BAN_commune_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_bandit_control > 0 }
											}
											add_to_variable = {
												BAN_bandit_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_cyrille_kill_bronsfeather = {
		icon = griffon
		activation = { has_country_flag = BAN_cyrille_target_bronsfeather }
		available = {
			always = no
		}
		cost = 0
		days_remove = 10
		days_mission_timeout = 30
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_auto_cyrille_kill_bronsfeather"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_auto_cyrille_kill_bronsfeather"
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
		}
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_kill_bronsfeather"
			remove_unit_leader = 69003
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_bronsfeather_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_kajetan_controls_state
						}
						clr_state_flag = BAN_kajetan_controls_state
						set_state_flag = BAN_blackrock_controls_state
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 4 }
							}
							add_to_variable = {
								BAN_bandit_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 3 }
								}
								add_to_variable = {
									BAN_bandit_control = -4
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 2 }
									}
									add_to_variable = {
										BAN_bandit_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control > 1 }
										}
										add_to_variable = {
											BAN_bandit_control = -2
										}
										add_to_variable = {
											BAN_noble_control = 1
										}
										add_to_variable = {
											BAN_commune_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_bandit_control > 0 }
											}
											add_to_variable = {
												BAN_bandit_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_cyrille_kill_hiperion = {
		icon = griffon
		activation = { has_country_flag = BAN_cyrille_target_hiperion }
		available = {
			always = no
		}
		cost = 0
		days_remove = 10
		days_mission_timeout = 30
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_auto_cyrille_kill_hiperion"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_auto_cyrille_kill_hiperion"
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
		}
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_kill_hiperion"
			remove_unit_leader = 69002
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_hiperion_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_ulrich_controls_state
						}
						clr_state_flag = BAN_ulrich_controls_state
						set_state_flag = BAN_blackrock_controls_state
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 4 }
							}
							add_to_variable = {
								BAN_bandit_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 3 }
								}
								add_to_variable = {
									BAN_bandit_control = -4
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 2 }
									}
									add_to_variable = {
										BAN_bandit_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control > 1 }
										}
										add_to_variable = {
											BAN_bandit_control = -2
										}
										add_to_variable = {
											BAN_noble_control = 1
										}
										add_to_variable = {
											BAN_commune_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_bandit_control > 0 }
											}
											add_to_variable = {
												BAN_bandit_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_cyrille_kill_tercel = {
		icon = griffon
		activation = { has_country_flag = BAN_cyrille_target_tercel }
		available = {
			has_country_flag = BAN_stop_assassination_flag
		}
		cost = 0
		days_remove = 10
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_kill_tercel"
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_tercel_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_gavrila_controls_state
						}
						clr_state_flag = BAN_gavrila_controls_state
						set_state_flag = BAN_reeve_controls_state
						if = {
							limit = {
								check_variable = { BAN_commune_control > 4 }
							}
							add_to_variable = {
								BAN_commune_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_bandit_control = 2
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_commune_control > 3 }
								}
								add_to_variable = {
									BAN_commune_control = -4
								}
								add_to_variable = {
									BAN_bandit_control = 2
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_commune_control > 2 }
									}
									add_to_variable = {
										BAN_commune_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = 1
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_commune_control > 1 }
										}
										add_to_variable = {
											BAN_commune_control = -2
										}
										add_to_variable = {
											BAN_bandit_control = 1
										}
										add_to_variable = {
											BAN_noble_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_commune_control > 0 }
											}
											add_to_variable = {
												BAN_commune_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_cyrille_kill_horndrogiannis = {
		icon = griffon
		activation = { has_country_flag = BAN_cyrille_target_horndrogiannis }
		available = {
			has_country_flag = BAN_stop_assassination_flag
		}
		cost = 0
		days_remove = 10
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_kill_horndrogiannis"
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_horndrogiannis_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_giannis_controls_state
						}
						clr_state_flag = BAN_giannis_controls_state
						set_state_flag = BAN_reeve_controls_state
						if = {
							limit = {
								check_variable = { BAN_commune_control > 4 }
							}
							add_to_variable = {
								BAN_commune_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_bandit_control = 2
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_commune_control > 3 }
								}
								add_to_variable = {
									BAN_commune_control = -4
								}
								add_to_variable = {
									BAN_bandit_control = 2
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_commune_control > 2 }
									}
									add_to_variable = {
										BAN_commune_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = 1
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_commune_control > 1 }
										}
										add_to_variable = {
											BAN_commune_control = -2
										}
										add_to_variable = {
											BAN_bandit_control = 1
										}
										add_to_variable = {
											BAN_noble_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_commune_control > 0 }
											}
											add_to_variable = {
												BAN_commune_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_cyrille_increase_446 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_cyrille_increase_446_flag }
		available = { always = no }
		days_mission_timeout = 20
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_increase_446"
			custom_effect_tooltip = BAN_add_3_noble_control_creeper_tt
			hidden_effect = {
				set_country_flag = BAN_auto_cyrille_increase_446_flag
				446 = {
					if = {
						limit = {
							check_variable = { BAN_bandit_control > 2 }
						}
						add_to_variable = {
							BAN_noble_control = 3
						}
						add_to_variable = {
							BAN_bandit_control = -3
						}
						else_if = {
							limit = {
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_noble_control = 1
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.45
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_cyrille_attack_446 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_cyrille_attack_446_flag }
		available = { always = no }
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_attack_446"
			custom_effect_tooltip = BAN_cyrille_attack_446
			hidden_effect = {
				set_country_flag = BAN_auto_cyrille_attack_446_flag
				if = {
					limit = {
						446 = { has_state_flag = BAN_actual_controller_bandits }
					}
					subtract_from_variable = {
						var = BAN_bandit_divisions
						value = 446.BAN_bandit_garrison
					}
					subtract_from_variable = {
						var = BAN_special_bandit_divisions
						value = 446.BAN_special_bandit_garrison
					}
					446 = {
						set_variable = {
							BAN_bandit_garrison = 0
						}
						set_variable = {
							BAN_special_bandit_garrison = 0
						}
						set_variable = {
							BAN_noble_garrison = 1
						}
						clr_state_flag = BAN_actual_controller_bandits
						clr_state_flag = BAN_blackrock_controls_state
						clr_state_flag = BAN_ulrich_controls_state
						clr_state_flag = BAN_dominik_controls_state
						clr_state_flag = BAN_kajetan_controls_state
						set_state_flag = BAN_actual_controller_nobles
						set_state_flag = BAN_cyrille_controls_state
					}
					else_if = { # will need an event for this at some point
						limit = {
							446 = { has_state_flag = BAN_actual_controller_communes }
						}
						if = {
							limit = {
								446 = {
									OR = {
										AND = {
											check_variable = { BAN_commune_garrison = 0 }
											check_variable = { BAN_special_commune_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_commune_garrison = 1 }
											check_variable = { BAN_special_commune_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_commune_garrison = 0 }
											check_variable = { BAN_special_commune_garrison = 1 }
										}
									}
								}
							}
							subtract_from_variable = {
								var = BAN_commune_divisions
								value = 446.BAN_commune_garrison
							}
							subtract_from_variable = {
								var = BAN_special_commune_divisions
								value = 446.BAN_special_commune_garrison
							}
							446 = {
								set_variable = {
									BAN_commune_garrison = 0
								}
								set_variable = {
									BAN_special_commune_garrison = 0
								}
								set_variable = {
									BAN_commune_garrison = 1
								}
								clr_state_flag = BAN_actual_controller_communes
								clr_state_flag = BAN_cyrille_controls_state
								clr_state_flag = BAN_silas_controls_state
								clr_state_flag = BAN_sir_controls_state
								set_state_flag = BAN_actual_controller_nobles
								set_state_flag = BAN_cyrille_controls_state
							}
							else = {
								add_to_variable = {
									BAN_noble_divisions = -2
								}
								add_to_variable = {
									BAN_commune_divisions = -1
								}
								446 = {
									add_to_variable = {
										BAN_commune_garrison = -1
									}
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.45
				}
				update_BAN_variables = yes
				if = {
					limit = {
						ROOT = { check_variable = { BAN_available_noble_divisions < 3 } }
					}
					activate_decision = BAN_auto_cyrille_recruit_division
					set_country_flag = BAN_auto_cyrille_recruiting
				}
			}
		}
	}
	BAN_auto_cyrille_increase_606 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_cyrille_increase_606_flag }
		available = { always = no }
		days_mission_timeout = 20
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_increase_606"
			custom_effect_tooltip = BAN_add_3_noble_control_banditplace_tt
			hidden_effect = {
				set_country_flag = BAN_auto_cyrille_increase_606_flag
				606 = {
					if = {
						limit = {
							check_variable = { BAN_bandit_control > 2 }
						}
						add_to_variable = {
							BAN_noble_control = 3
						}
						add_to_variable = {
							BAN_bandit_control = -3
						}
						else_if = {
							limit = {
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_noble_control = 1
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.45
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_cyrille_attack_606 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_cyrille_attack_606_flag }
		available = { always = no }
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_attack_606"
			custom_effect_tooltip = BAN_cyrille_attack_606
			hidden_effect = {
				set_country_flag = BAN_auto_cyrille_attack_606_flag
				if = {
					limit = {
						606 = { has_state_flag = BAN_actual_controller_bandits }
					}
					subtract_from_variable = {
						var = BAN_bandit_divisions
						value = 606.BAN_bandit_garrison
					}
					subtract_from_variable = {
						var = BAN_special_bandit_divisions
						value = 606.BAN_special_bandit_garrison
					}
					606 = {
						set_variable = {
							BAN_bandit_garrison = 0
						}
						set_variable = {
							BAN_special_bandit_garrison = 0
						}
						set_variable = {
							BAN_commune_garrison = 1
						}
						clr_state_flag = BAN_actual_controller_bandits
						clr_state_flag = BAN_blackrock_controls_state
						clr_state_flag = BAN_ulrich_controls_state
						clr_state_flag = BAN_dominik_controls_state
						clr_state_flag = BAN_kajetan_controls_state
						set_state_flag = BAN_actual_controller_nobles
						set_state_flag = BAN_cyrille_controls_state
					}
					else_if = { # will need an event for this at some point
						limit = {
							606 = { has_state_flag = BAN_actual_controller_communes }
						}
						if = {
							limit = {
								606 = {
									OR = {
										AND = {
											check_variable = { BAN_commune_garrison = 0 }
											check_variable = { BAN_special_commune_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_commune_garrison = 1 }
											check_variable = { BAN_special_commune_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_commune_garrison = 0 }
											check_variable = { BAN_special_commune_garrison = 1 }
										}
									}
								}
							}
							subtract_from_variable = {
								var = BAN_commune_divisions
								value = 606.BAN_commune_garrison
							}
							subtract_from_variable = {
								var = BAN_special_commune_divisions
								value = 606.BAN_special_commune_garrison
							}
							606 = {
								set_variable = {
									BAN_commune_garrison = 0
								}
								set_variable = {
									BAN_special_commune_garrison = 0
								}
								set_variable = {
									BAN_noble_garrison = 1
								}
								clr_state_flag = BAN_actual_controller_communes
								clr_state_flag = BAN_cyrille_controls_state
								clr_state_flag = BAN_silas_controls_state
								clr_state_flag = BAN_sir_controls_state
								set_state_flag = BAN_actual_controller_nobles
								set_state_flag = BAN_cyrille_controls_state
							}
							else = {
								add_to_variable = {
									BAN_noble_divisions = -2
								}
								add_to_variable = {
									BAN_commune_divisions = -1
								}
								606 = {
									add_to_variable = {
										BAN_commune_garrison = -1
									}
								}
								if = {
									limit = {
										606 = { 
											OR = { 
												has_state_flag = BAN_actual_controller_bandits 
												AND = { 
													ROOT = { has_completed_focus = BAN_oppose_communism } has_state_flag = BAN_actual_controller_communes
												}
											}
										}
									}
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.45
				}
				update_BAN_variables = yes
				if = {
					limit = {
						ROOT = { check_variable = { BAN_available_noble_divisions < 3 } }
					}
					activate_decision = BAN_auto_cyrille_recruit_division
					set_country_flag = BAN_auto_cyrille_recruiting
				}
			}
		}
	}
	BAN_auto_cyrille_increase_490 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_cyrille_increase_490_flag }
		available = { always = no }
		days_mission_timeout = 20
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_increase_490"
			custom_effect_tooltip = BAN_add_3_noble_control_blackhollow_tt
			hidden_effect = {
				set_country_flag = BAN_auto_cyrille_increase_490_flag
				490 = {
					if = {
						limit = {
							check_variable = { BAN_bandit_control > 2 }
						}
						add_to_variable = {
							BAN_noble_control = 3
						}
						add_to_variable = {
							BAN_bandit_control = -3
						}
						else_if = {
							limit = {
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_noble_control = 1
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.45
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_cyrille_attack_490 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_cyrille_attack_490_flag }
		available = { always = no }
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_attack_490"
			custom_effect_tooltip = BAN_cyrille_attack_490
			hidden_effect = {
				set_country_flag = BAN_auto_cyrille_attack_490_flag
				if = {
					limit = {
						490 = { has_state_flag = BAN_actual_controller_bandits }
					}
					subtract_from_variable = {
						var = BAN_bandit_divisions
						value = 490.BAN_bandit_garrison
					}
					subtract_from_variable = {
						var = BAN_special_bandit_divisions
						value = 490.BAN_special_bandit_garrison
					}
					490 = {
						set_variable = {
							BAN_bandit_garrison = 0
						}
						set_variable = {
							BAN_special_bandit_garrison = 0
						}
						set_variable = {
							BAN_commune_garrison = 1
						}
						clr_state_flag = BAN_actual_controller_bandits
						clr_state_flag = BAN_blackrock_controls_state
						clr_state_flag = BAN_ulrich_controls_state
						clr_state_flag = BAN_dominik_controls_state
						clr_state_flag = BAN_kajetan_controls_state
						set_state_flag = BAN_actual_controller_nobles
						set_state_flag = BAN_cyrille_controls_state
					}
					else_if = { # will need an event for this at some point
						limit = {
							490 = { has_state_flag = BAN_actual_controller_communes }
						}
						if = {
							limit = {
								490 = {
									OR = {
										AND = {
											check_variable = { BAN_commune_garrison = 0 }
											check_variable = { BAN_special_commune_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_commune_garrison = 1 }
											check_variable = { BAN_special_commune_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_commune_garrison = 0 }
											check_variable = { BAN_special_commune_garrison = 1 }
										}
									}
								}
							}
							subtract_from_variable = {
								var = BAN_commune_divisions
								value = 490.BAN_commune_garrison
							}
							subtract_from_variable = {
								var = BAN_special_commune_divisions
								value = 490.BAN_special_commune_garrison
							}
							490 = {
								set_variable = {
									BAN_commune_garrison = 0
								}
								set_variable = {
									BAN_special_commune_garrison = 0
								}
								set_variable = {
									BAN_noble_garrison = 1
								}
								clr_state_flag = BAN_actual_controller_communes
								clr_state_flag = BAN_cyrille_controls_state
								clr_state_flag = BAN_silas_controls_state
								clr_state_flag = BAN_sir_controls_state
								set_state_flag = BAN_actual_controller_nobles
								set_state_flag = BAN_cyrille_controls_state
							}
							else = {
								add_to_variable = {
									BAN_noble_divisions = -2
								}
								add_to_variable = {
									BAN_commune_divisions = -1
								}
								490 = {
									add_to_variable = {
										BAN_commune_garrison = -1
									}
								}
								if = {
									limit = {
										490 = { 
											OR = { 
												has_state_flag = BAN_actual_controller_bandits 
												AND = { 
													ROOT = { has_completed_focus = BAN_oppose_communism } has_state_flag = BAN_actual_controller_communes
												}
											}
										}
									}
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.45
				}
				update_BAN_variables = yes
				if = {
					limit = {
						ROOT = { check_variable = { BAN_available_noble_divisions < 3 } }
					}
					activate_decision = BAN_auto_cyrille_recruit_division
					set_country_flag = BAN_auto_cyrille_recruiting
				}
			}
		}
	}
	BAN_auto_cyrille_increase_489 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_cyrille_increase_489_flag }
		available = { always = no }
		days_mission_timeout = 20
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_increase_489"
			custom_effect_tooltip = BAN_add_3_noble_control_naniwich_tt
			hidden_effect = {
				set_country_flag = BAN_auto_cyrille_increase_489_flag
				489 = {
					if = {
						limit = {
							check_variable = { BAN_bandit_control > 2 }
						}
						add_to_variable = {
							BAN_noble_control = 3
						}
						add_to_variable = {
							BAN_bandit_control = -3
						}
						else_if = {
							limit = {
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_noble_control = 1
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.45
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_cyrille_attack_489 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_cyrille_attack_489_flag }
		available = { always = no }
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_attack_489"
			custom_effect_tooltip = BAN_cyrille_attack_489
			hidden_effect = {
				set_country_flag = BAN_auto_cyrille_attack_489_flag
				if = {
					limit = {
						489 = { has_state_flag = BAN_actual_controller_bandits }
					}
					subtract_from_variable = {
						var = BAN_bandit_divisions
						value = 489.BAN_bandit_garrison
					}
					subtract_from_variable = {
						var = BAN_special_bandit_divisions
						value = 489.BAN_special_bandit_garrison
					}
					489 = {
						set_variable = {
							BAN_bandit_garrison = 0
						}
						set_variable = {
							BAN_special_bandit_garrison = 0
						}
						set_variable = {
							BAN_commune_garrison = 1
						}
						clr_state_flag = BAN_actual_controller_bandits
						clr_state_flag = BAN_blackrock_controls_state
						clr_state_flag = BAN_ulrich_controls_state
						clr_state_flag = BAN_dominik_controls_state
						clr_state_flag = BAN_kajetan_controls_state
						set_state_flag = BAN_actual_controller_monlrd
						set_state_flag = BAN_cyrille_controls_state
					}
					else_if = { # will need an event for this at some point
						limit = {
							489 = { has_state_flag = BAN_actual_controller_communes }
						}
						if = {
							limit = {
								489 = {
									OR = {
										AND = {
											check_variable = { BAN_commune_garrison = 0 }
											check_variable = { BAN_special_commune_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_commune_garrison = 1 }
											check_variable = { BAN_special_commune_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_commune_garrison = 0 }
											check_variable = { BAN_special_commune_garrison = 1 }
										}
									}
								}
							}
							subtract_from_variable = {
								var = BAN_commune_divisions
								value = 489.BAN_commune_garrison
							}
							subtract_from_variable = {
								var = BAN_special_commune_divisions
								value = 489.BAN_special_commune_garrison
							}
							489 = {
								set_variable = {
									BAN_commune_garrison = 0
								}
								set_variable = {
									BAN_special_commune_garrison = 0
								}
								set_variable = {
									BAN_commune_garrison = 1
								}
								clr_state_flag = BAN_actual_controller_communes
								clr_state_flag = BAN_cyrille_controls_state
								clr_state_flag = BAN_silas_controls_state
								clr_state_flag = BAN_sir_controls_state
								set_state_flag = BAN_actual_controller_nobles
								set_state_flag = BAN_cyrille_controls_state
							}
							else = {
								add_to_variable = {
									BAN_nobles_divisions = -2
								}
								add_to_variable = {
									BAN_commune_divisions = -1
								}
								489 = {
									add_to_variable = {
										BAN_commune_garrison = -1
									}
								}
								if = {
									limit = {
										489 = { 
											OR = { 
												has_state_flag = BAN_actual_controller_bandits 
												AND = { 
													ROOT = { has_completed_focus = BAN_oppose_communism } has_state_flag = BAN_actual_controller_communes
												}
											}
										}
									}
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.45
				}
				update_BAN_variables = yes
				if = {
					limit = {
						ROOT = { check_variable = { BAN_available_noble_divisions < 3 } }
					}
					activate_decision = BAN_auto_cyrille_recruit_division
					set_country_flag = BAN_auto_cyrille_recruiting
				}
			}
		}
	}
	BAN_auto_cyrille_increase_532 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_cyrille_increase_532_flag }
		available = { always = no }
		days_mission_timeout = 20
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_increase_532"
			custom_effect_tooltip = BAN_add_3_noble_control_murkbeak_tt
			hidden_effect = {
				set_country_flag = BAN_auto_cyrille_increase_532_flag
				532 = {
					if = {
						limit = {
							check_variable = { BAN_bandit_control > 2 }
						}
						add_to_variable = {
							BAN_noble_control = 3
						}
						add_to_variable = {
							BAN_bandit_control = -3
						}
						else_if = {
							limit = {
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_noble_control = 1
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.45
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_auto_cyrille_attack_532 = {
		icon = griffon
		activation = { has_country_flag = BAN_auto_cyrille_attack_532_flag }
		available = { always = no }
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_attack_532"
			custom_effect_tooltip = BAN_cyrille_attack_532
			hidden_effect = {
				set_country_flag = BAN_auto_cyrille_attack_532_flag
				if = {
					limit = {
						532 = { has_state_flag = BAN_actual_controller_bandits }
					}
					subtract_from_variable = {
						var = BAN_bandit_divisions
						value = 532.BAN_bandit_garrison
					}
					subtract_from_variable = {
						var = BAN_special_bandit_divisions
						value = 532.BAN_special_bandit_garrison
					}
					532 = {
						set_variable = {
							BAN_bandit_garrison = 0
						}
						set_variable = {
							BAN_special_bandit_garrison = 0
						}
						set_variable = {
							BAN_commune_garrison = 1
						}
						clr_state_flag = BAN_actual_controller_bandits
						clr_state_flag = BAN_blackrock_controls_state
						clr_state_flag = BAN_ulrich_controls_state
						clr_state_flag = BAN_dominik_controls_state
						clr_state_flag = BAN_kajetan_controls_state
						set_state_flag = BAN_actual_controller_nobles
						set_state_flag = BAN_cyrille_controls_state
					}
					else_if = { # will need an event for this at some point
						limit = {
							532 = { has_state_flag = BAN_actual_controller_communes }
						}
						if = {
							limit = {
								532 = {
									OR = {
										AND = {
											check_variable = { BAN_commune_garrison = 0 }
											check_variable = { BAN_special_commune_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_commune_garrison = 1 }
											check_variable = { BAN_special_commune_garrison = 0 }
										}
										AND = {
											check_variable = { BAN_commune_garrison = 0 }
											check_variable = { BAN_special_commune_garrison = 1 }
										}
									}
								}
							}
							subtract_from_variable = {
								var = BAN_commune_divisions
								value = 532.BAN_commune_garrison
							}
							subtract_from_variable = {
								var = BAN_special_commune_divisions
								value = 532.BAN_special_commune_garrison
							}
							532 = {
								set_variable = {
									BAN_commune_garrison = 0
								}
								set_variable = {
									BAN_special_commune_garrison = 0
								}
								set_variable = {
									BAN_commune_garrison = 1
								}
								clr_state_flag = BAN_actual_controller_communes
								clr_state_flag = BAN_cyrille_controls_state
								clr_state_flag = BAN_silas_controls_state
								clr_state_flag = BAN_sir_controls_state
								set_state_flag = BAN_actual_controller_nobles
								set_state_flag = BAN_cyrille_controls_state
							}
							else = {
								add_to_variable = {
									BAN_noble_divisions = -2
								}
								add_to_variable = {
									BAN_commune_divisions = -1
								}
								532 = {
									add_to_variable = {
										BAN_commune_garrison = -1
									}
								}
								if = {
									limit = {
										532 = { 
											OR = { 
												has_state_flag = BAN_actual_controller_bandits 
												AND = { 
													ROOT = { has_completed_focus = BAN_oppose_communism } has_state_flag = BAN_actual_controller_communes
												}
											}
										}
									}
								}
							}
						}
					}
				}
				country_event = {
					id = bandit.45
				}
				update_BAN_variables = yes
				if = {
					limit = {
						ROOT = { check_variable = { BAN_available_noble_divisions < 3 } }
					}
					activate_decision = BAN_auto_cyrille_recruit_division
					set_country_flag = BAN_auto_cyrille_recruiting
				}
			}
		}
	}
	BAN_auto_cyrille_recruit_division = {
		icon = rifles
		activation = {
			has_country_flag = BAN_auto_cyrille_recruiting
		}
		available = {
			always = no
		}
		days_mission_timeout = 30
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout BAN_auto_cyrille_recruit_division"
			custom_effect_tooltip = BAN_recruit_noble_division
			add_to_variable = { BAN_noble_divisions = 1 }
			clr_country_flag = BAN_auto_cyrille_recruiting
		}
	}

	BAN_strengthen_nobles_decision_murkbeak = { # i was originally gonna make a dynamic decision to save space but then i got lazy
		icon = griffon
		ai_will_do = {
			factor = 10
		}
		highlight_states = {
			state = 532
		}
		visible = {
			has_completed_focus = BAN_noble_spirit
			owns_state = 532
		}
		available = {
			controls_state = 532
			custom_trigger_tooltip = {
				tooltip = BAN_can_strengthen_tt
				hidden_trigger = {
					OR = {
						AND = {
							ROOT = { has_completed_focus = BAN_oppose_communism }
							OR = {
								532 = {
										check_variable = { BAN_commune_control > 0 }
								}
								532 = {
										check_variable = { BAN_bandit_control > 0 }
								}
							}
						}
						AND = {
							ROOT = { NOT = { has_completed_focus = BAN_oppose_communism } }
							532 = {
								check_variable = { BAN_bandit_control > 0 }
							}
						}
					}
				}
			}
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		cost = 0
		days_remove = 20
		complete_effect = {
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_strengthen_nobles_decision_murkbeak"
			custom_effect_tooltip = BAN_add_3_noble_control_murkbeak_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			hidden_effect = {
				532 = {
					if = {
						limit = {
							ROOT = { has_completed_focus = BAN_oppose_communism }
						}
						if = {
							limit = {
								check_variable = { BAN_commune_control > 0 }
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_noble_control = 3
							}
							add_to_variable = {
								BAN_commune_control = -1
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_commune_control > 1 }
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_noble_control = 3
								}
								add_to_variable = {
									BAN_commune_control = -2
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
								else_if = {
									limit = {
										check_variable = { BAN_commune_control > 0 }
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_noble_control = 2
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									add_to_variable = {
										BAN_commune_control = -1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control = 0 }
											check_variable = { BAN_commune_control > 2 }
										}
										add_to_variable = {
											BAN_noble_control = 3
										}
										add_to_variable = {
											BAN_commune_control = -3
										}
										else_if = {
											limit = {
												check_variable = { BAN_commune_control > 1 }
												check_variable = { BAN_bandit_control = 0 }
											}
											add_to_variable = {
												BAN_noble_control = 2
											}
											add_to_variable = {
												BAN_commune_control = -2
											}
											else_if = {
												limit = {
													check_variable = { BAN_commune_control > 0 }
													check_variable = { BAN_bandit_control = 0 }
												}
												add_to_variable = {
													BAN_noble_control = 1
												}
												add_to_variable = {
													BAN_commune_control = -1
												}
												else_if = {
													limit = {
														check_variable = { BAN_commune_control = 0 }
														check_variable = { BAN_bandit_control > 2 }
													}
													add_to_variable = {
														BAN_noble_control = 3
													}
													add_to_variable = {
														BAN_bandit_control = -3
													}
													else_if = {
														limit = {
															check_variable = { BAN_commune_control = 0 }
															check_variable = { BAN_bandit_control > 1 }
														}
														add_to_variable = {
															BAN_noble_control = 2
														}
														add_to_variable = {
															BAN_bandit_control = -2
														}
														else_if = {
															limit = {
																check_variable = { BAN_commune_control = 0 }
																check_variable = { BAN_bandit_control > 0 }
															}
															add_to_variable = {
																BAN_noble_control = 1
															}
															add_to_variable = {
																BAN_bandit_control = -1
															}
														}
													}
												}
											}
										}
									}
								}
							}
						}
					}
					if = {
						limit = {
							ROOT = { NOT = { has_completed_focus = BAN_oppose_communism } }
						}
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 2 }
							}
							add_to_variable = {
								BAN_noble_control = 3
							}
							add_to_variable = {
								BAN_bandit_control = -3
							}
							else_if = {
									limit = {
									check_variable = { BAN_bandit_control > 1 }
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								add_to_variable = {
									BAN_bandit_control = -2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									else = {
										#
									}
								}
							}
						}	
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_strengthen_nobles_decision_naniwich = {
		icon = griffon
		ai_will_do = {
			factor = 10
		}
		highlight_states = {
			state = 489
		}
		visible = {
			has_completed_focus = BAN_noble_spirit
			owns_state = 489
		}
		available = {
			controls_state = 489
			custom_trigger_tooltip = {
				tooltip = BAN_can_strengthen_tt
				hidden_trigger = {
					OR = {
						AND = {
							ROOT = { has_completed_focus = BAN_oppose_communism }
							OR = {
								489 = {
										check_variable = { BAN_commune_control > 0 }
								}
								489 = {
										check_variable = { BAN_bandit_control > 0 }
								}
							}
						}
						AND = {
							ROOT = { NOT = { has_completed_focus = BAN_oppose_communism } }
							489 = {
								check_variable = { BAN_bandit_control > 0 }
							}
						}
					}
				}
			}
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		cost = 0
		days_remove = 20
		complete_effect = {
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_strengthen_nobles_decision_naniwich"
			custom_effect_tooltip = BAN_add_3_noble_control_naniwich_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			hidden_effect = {
				489 = {
					if = {
						limit = {
							ROOT = { has_completed_focus = BAN_oppose_communism }
						}
						if = {
							limit = {
								check_variable = { BAN_commune_control > 0 }
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_noble_control = 3
							}
							add_to_variable = {
								BAN_commune_control = -1
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_commune_control > 1 }
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_noble_control = 3
								}
								add_to_variable = {
									BAN_commune_control = -2
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
								else_if = {
									limit = {
										check_variable = { BAN_commune_control > 0 }
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_noble_control = 2
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									add_to_variable = {
										BAN_commune_control = -1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control = 0 }
											check_variable = { BAN_commune_control > 2 }
										}
										add_to_variable = {
											BAN_noble_control = 3
										}
										add_to_variable = {
											BAN_commune_control = -3
										}
										else_if = {
											limit = {
												check_variable = { BAN_commune_control > 1 }
												check_variable = { BAN_bandit_control = 0 }
											}
											add_to_variable = {
												BAN_noble_control = 2
											}
											add_to_variable = {
												BAN_commune_control = -2
											}
											else_if = {
												limit = {
													check_variable = { BAN_commune_control > 0 }
													check_variable = { BAN_bandit_control = 0 }
												}
												add_to_variable = {
													BAN_noble_control = 1
												}
												add_to_variable = {
													BAN_commune_control = -1
												}
												else_if = {
													limit = {
														check_variable = { BAN_commune_control = 0 }
														check_variable = { BAN_bandit_control > 2 }
													}
													add_to_variable = {
														BAN_noble_control = 3
													}
													add_to_variable = {
														BAN_bandit_control = -3
													}
													else_if = {
														limit = {
															check_variable = { BAN_commune_control = 0 }
															check_variable = { BAN_bandit_control > 1 }
														}
														add_to_variable = {
															BAN_noble_control = 2
														}
														add_to_variable = {
															BAN_bandit_control = -2
														}
														else_if = {
															limit = {
																check_variable = { BAN_commune_control = 0 }
																check_variable = { BAN_bandit_control > 0 }
															}
															add_to_variable = {
																BAN_noble_control = 1
															}
															add_to_variable = {
																BAN_bandit_control = -1
															}
														}
													}
												}
											}
										}
									}
								}
							}
						}
					}
					if = {
						limit = {
							ROOT = { NOT = { has_completed_focus = BAN_oppose_communism } }
						}
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 2 }
							}
							add_to_variable = {
								BAN_noble_control = 3
							}
							add_to_variable = {
								BAN_bandit_control = -3
							}
							else_if = {
									limit = {
									check_variable = { BAN_bandit_control > 1 }
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								add_to_variable = {
									BAN_bandit_control = -2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									else = {
										#
									}
								}
							}
						}	
					}
					update_BAN_variables = yes
				}
			}
		}
	}
	BAN_strengthen_nobles_decision_creeper = {
		icon = griffon
		ai_will_do = {
			factor = 10
		}
		highlight_states = {
			state = 446
		}
		visible = {
			has_completed_focus = BAN_noble_spirit
			owns_state = 446
		}
		available = {
			controls_state = 446
			custom_trigger_tooltip = {
				tooltip = BAN_can_strengthen_tt
				hidden_trigger = {
					OR = {
						AND = {
							ROOT = { has_completed_focus = BAN_oppose_communism }
							OR = {
								446 = {
										check_variable = { BAN_commune_control > 0 }
								}
								446 = {
										check_variable = { BAN_bandit_control > 0 }
								}
							}
						}
						AND = {
							ROOT = { NOT = { has_completed_focus = BAN_oppose_communism } }
							446 = {
								check_variable = { BAN_bandit_control > 0 }
							}
						}
					}
				}
			}
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		cost = 0
		days_remove = 20
		complete_effect = {
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_strengthen_nobles_decision_creeper"
			custom_effect_tooltip = BAN_add_3_noble_control_creeper_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			hidden_effect = {
				446 = {
					if = {
						limit = {
							ROOT = { has_completed_focus = BAN_oppose_communism }
						}
						if = {
							limit = {
								check_variable = { BAN_commune_control > 0 }
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_noble_control = 3
							}
							add_to_variable = {
								BAN_commune_control = -1
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_commune_control > 1 }
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_noble_control = 3
								}
								add_to_variable = {
									BAN_commune_control = -2
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
								else_if = {
									limit = {
										check_variable = { BAN_commune_control > 0 }
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_noble_control = 2
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									add_to_variable = {
										BAN_commune_control = -1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control = 0 }
											check_variable = { BAN_commune_control > 2 }
										}
										add_to_variable = {
											BAN_noble_control = 3
										}
										add_to_variable = {
											BAN_commune_control = -3
										}
										else_if = {
											limit = {
												check_variable = { BAN_commune_control > 1 }
												check_variable = { BAN_bandit_control = 0 }
											}
											add_to_variable = {
												BAN_noble_control = 2
											}
											add_to_variable = {
												BAN_commune_control = -2
											}
											else_if = {
												limit = {
													check_variable = { BAN_commune_control > 0 }
													check_variable = { BAN_bandit_control = 0 }
												}
												add_to_variable = {
													BAN_noble_control = 1
												}
												add_to_variable = {
													BAN_commune_control = -1
												}
												else_if = {
													limit = {
														check_variable = { BAN_commune_control = 0 }
														check_variable = { BAN_bandit_control > 2 }
													}
													add_to_variable = {
														BAN_noble_control = 3
													}
													add_to_variable = {
														BAN_bandit_control = -3
													}
													else_if = {
														limit = {
															check_variable = { BAN_commune_control = 0 }
															check_variable = { BAN_bandit_control > 1 }
														}
														add_to_variable = {
															BAN_noble_control = 2
														}
														add_to_variable = {
															BAN_bandit_control = -2
														}
														else_if = {
															limit = {
																check_variable = { BAN_commune_control = 0 }
																check_variable = { BAN_bandit_control > 0 }
															}
															add_to_variable = {
																BAN_noble_control = 1
															}
															add_to_variable = {
																BAN_bandit_control = -1
															}
														}
													}
												}
											}
										}
									}
								}
							}
						}
					}
					if = {
						limit = {
							ROOT = { NOT = { has_completed_focus = BAN_oppose_communism } }
						}
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 2 }
							}
							add_to_variable = {
								BAN_noble_control = 3
							}
							add_to_variable = {
								BAN_bandit_control = -3
							}
							else_if = {
									limit = {
									check_variable = { BAN_bandit_control > 1 }
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								add_to_variable = {
									BAN_bandit_control = -2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									else = {
										#
									}
								}
							}
						}	
					}
					update_BAN_variables = yes
				}
			}
		}
	}
	BAN_strengthen_nobles_decision_banditplace = {
		icon = griffon
		ai_will_do = {
			factor = 10
		}
		highlight_states = {
			state = 606
		}
		visible = {
			has_completed_focus = BAN_noble_spirit
			owns_state = 606
		}
		available = {
			controls_state = 606
			custom_trigger_tooltip = {
				tooltip = BAN_can_strengthen_tt
				hidden_trigger = {
					OR = {
						AND = {
							ROOT = { has_completed_focus = BAN_oppose_communism }
							OR = {
								606 = {
										check_variable = { BAN_commune_control > 0 }
								}
								606 = {
										check_variable = { BAN_bandit_control > 0 }
								}
							}
						}
						AND = {
							ROOT = { NOT = { has_completed_focus = BAN_oppose_communism } }
							606 = {
								check_variable = { BAN_bandit_control > 0 }
							}
						}
					}
				}
			}
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		cost = 0
		days_remove = 20
		complete_effect = {
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_strengthen_nobles_decision_banditplace"
			custom_effect_tooltip = BAN_add_3_noble_control_banditplace_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			hidden_effect = {
				606 = {
					if = {
						limit = {
							ROOT = { has_completed_focus = BAN_oppose_communism }
						}
						if = {
							limit = {
								check_variable = { BAN_commune_control > 0 }
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_noble_control = 3
							}
							add_to_variable = {
								BAN_commune_control = -1
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_commune_control > 1 }
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_noble_control = 3
								}
								add_to_variable = {
									BAN_commune_control = -2
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
								else_if = {
									limit = {
										check_variable = { BAN_commune_control > 0 }
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_noble_control = 2
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									add_to_variable = {
										BAN_commune_control = -1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control = 0 }
											check_variable = { BAN_commune_control > 2 }
										}
										add_to_variable = {
											BAN_noble_control = 3
										}
										add_to_variable = {
											BAN_commune_control = -3
										}
										else_if = {
											limit = {
												check_variable = { BAN_commune_control > 1 }
												check_variable = { BAN_bandit_control = 0 }
											}
											add_to_variable = {
												BAN_noble_control = 2
											}
											add_to_variable = {
												BAN_commune_control = -2
											}
											else_if = {
												limit = {
													check_variable = { BAN_commune_control > 0 }
													check_variable = { BAN_bandit_control = 0 }
												}
												add_to_variable = {
													BAN_noble_control = 1
												}
												add_to_variable = {
													BAN_commune_control = -1
												}
												else_if = {
													limit = {
														check_variable = { BAN_commune_control = 0 }
														check_variable = { BAN_bandit_control > 2 }
													}
													add_to_variable = {
														BAN_noble_control = 3
													}
													add_to_variable = {
														BAN_bandit_control = -3
													}
													else_if = {
														limit = {
															check_variable = { BAN_commune_control = 0 }
															check_variable = { BAN_bandit_control > 1 }
														}
														add_to_variable = {
															BAN_noble_control = 2
														}
														add_to_variable = {
															BAN_bandit_control = -2
														}
														else_if = {
															limit = {
																check_variable = { BAN_commune_control = 0 }
																check_variable = { BAN_bandit_control > 0 }
															}
															add_to_variable = {
																BAN_noble_control = 1
															}
															add_to_variable = {
																BAN_bandit_control = -1
															}
														}
													}
												}
											}
										}
									}
								}
							}
						}
					}
					if = {
						limit = {
							ROOT = { NOT = { has_completed_focus = BAN_oppose_communism } }
						}
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 2 }
							}
							add_to_variable = {
								BAN_noble_control = 3
							}
							add_to_variable = {
								BAN_bandit_control = -3
							}
							else_if = {
									limit = {
									check_variable = { BAN_bandit_control > 1 }
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								add_to_variable = {
									BAN_bandit_control = -2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									else = {
										#
									}
								}
							}
						}	
					}
					update_BAN_variables = yes
				}
			}
		}
	}
	BAN_strengthen_nobles_decision_blackhollow = {
		icon = griffon
		ai_will_do = {
			factor = 10
		}
		highlight_states = {
			state = 490
		}
		visible = {
			has_completed_focus = BAN_noble_spirit
			owns_state = 490
		}
		available = {
			controls_state = 490
			custom_trigger_tooltip = {
				tooltip = BAN_can_strengthen_tt
				hidden_trigger = {
					OR = {
						AND = {
							ROOT = { has_completed_focus = BAN_oppose_communism }
							OR = {
								490 = {
									check_variable = { BAN_commune_control > 0 }
								}
								490 = {
									check_variable = { BAN_bandit_control > 0 }
								}
							}
						}
						AND = {
							ROOT = { NOT = { has_completed_focus = BAN_oppose_communism } }
							490 = {
								check_variable = { BAN_bandit_control > 0 }
							}
						}
					}
				}
			}
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		cost = 0
		days_remove = 20
		complete_effect = {
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_strengthen_nobles_decision_blackhollow"
			custom_effect_tooltip = BAN_add_3_noble_control_blackhollow_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			hidden_effect = {
				490 = {
					if = {
						limit = {
							ROOT = { has_completed_focus = BAN_oppose_communism }
						}
						if = {
							limit = {
								check_variable = { BAN_commune_control > 0 }
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_noble_control = 3
							}
							add_to_variable = {
								BAN_commune_control = -1
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_commune_control > 1 }
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_noble_control = 3
								}
								add_to_variable = {
									BAN_commune_control = -2
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
								else_if = {
									limit = {
										check_variable = { BAN_commune_control > 0 }
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_noble_control = 2
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									add_to_variable = {
										BAN_commune_control = -1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control = 0 }
											check_variable = { BAN_commune_control > 2 }
										}
										add_to_variable = {
											BAN_noble_control = 3
										}
										add_to_variable = {
											BAN_commune_control = -3
										}
										else_if = {
											limit = {
												check_variable = { BAN_commune_control > 1 }
												check_variable = { BAN_bandit_control = 0 }
											}
											add_to_variable = {
												BAN_noble_control = 2
											}
											add_to_variable = {
												BAN_commune_control = -2
											}
											else_if = {
												limit = {
													check_variable = { BAN_commune_control > 0 }
													check_variable = { BAN_bandit_control = 0 }
												}
												add_to_variable = {
													BAN_noble_control = 1
												}
												add_to_variable = {
													BAN_commune_control = -1
												}
												else_if = {
													limit = {
														check_variable = { BAN_commune_control = 0 }
														check_variable = { BAN_bandit_control > 2 }
													}
													add_to_variable = {
														BAN_noble_control = 3
													}
													add_to_variable = {
														BAN_bandit_control = -3
													}
													else_if = {
														limit = {
															check_variable = { BAN_commune_control = 0 }
															check_variable = { BAN_bandit_control > 1 }
														}
														add_to_variable = {
															BAN_noble_control = 2
														}
														add_to_variable = {
															BAN_bandit_control = -2
														}
														else_if = {
															limit = {
																check_variable = { BAN_commune_control = 0 }
																check_variable = { BAN_bandit_control > 0 }
															}
															add_to_variable = {
																BAN_noble_control = 1
															}
															add_to_variable = {
																BAN_bandit_control = -1
															}
														}
													}
												}
											}
										}
									}
								}
							}
						}
					}
					if = {
						limit = {
							ROOT = { NOT = { has_completed_focus = BAN_oppose_communism } }
						}
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 2 }
							}
							add_to_variable = {
								BAN_noble_control = 3
							}
							add_to_variable = {
								BAN_bandit_control = -3
							}
							else_if = {
									limit = {
									check_variable = { BAN_bandit_control > 1 }
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								add_to_variable = {
									BAN_bandit_control = -2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									else = {
										#
									}
								}
							}
						}	
					}
					update_BAN_variables = yes
				}
			}
		}
	}
	BAN_recruit_noble_division_decision = {
		icon = rifles
		visible = {
			has_completed_focus = BAN_noble_spirit
		}
		available = {
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		days_remove = 30
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BAN_recruit_noble_division"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_recruit_noble_division"
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			custom_effect_tooltip = BAN_recruit_noble_division
			add_to_variable = { BAN_noble_divisions = 1 }
		}
	}
	BAN_strengthen_communes_decision_murkbeak = {
		icon = griffon
		ai_will_do = {
			factor = 10
		}
		highlight_states = {
			state = 532
		}
		visible = {
			has_completed_focus = BAN_revolutionary_spirit
			owns_state = 532
		}
		available = {
			controls_state = 532
			custom_trigger_tooltip = {
				tooltip = BAN_can_strengthen_tt
				hidden_trigger = {
					OR = {
						AND = {
							ROOT = { has_completed_focus = BAN_no_compromise }
							OR = {
								532 = {
									check_variable = { BAN_noble_control > 0 }
								}
								532 = {
									check_variable = { BAN_bandit_control > 0 }
								}
							}
						}
						AND = {
							ROOT = { NOT = { has_completed_focus = BAN_no_compromise } }
							532 = {
								check_variable = { BAN_bandit_control > 0 }
							}
						}
					}
				}
			}
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		cost = 0
		days_remove = 20
		complete_effect = {
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_strengthen_communes_decision_murkbeak"
			custom_effect_tooltip = BAN_add_3_commune_control_murkbeak_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			hidden_effect = {
				532 = {
					if = {
						limit = {
							ROOT = { has_completed_focus = BAN_no_compromise }
						}
						if = {
							limit = {
								check_variable = { BAN_noble_control > 0 }
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_commune_control = 3
							}
							add_to_variable = {
								BAN_noble_control = -1
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_noble_control > 1 }
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_commune_control = 3
								}
								add_to_variable = {
									BAN_noble_control = -2
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
								else_if = {
									limit = {
										check_variable = { BAN_noble_control > 0 }
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_commune_control = 2
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									add_to_variable = {
										BAN_noble_control = -1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control = 0 }
											check_variable = { BAN_noble_control > 2 }
										}
										add_to_variable = {
											BAN_commune_control = 3
										}
										add_to_variable = {
											BAN_noble_control = -3
										}
										else_if = {
											limit = {
												check_variable = { BAN_bandit_control = 0 }
												check_variable = { BAN_noble_control > 1 }
											}
											add_to_variable = {
												BAN_commune_control = 2
											}
											add_to_variable = {
												BAN_noble_control = -2
											}
											else_if = {
												limit = {
													check_variable = { BAN_bandit_control = 0 }
													check_variable = { BAN_noble_control > 0 }
												}
												add_to_variable = {
													BAN_commune_control = 1
												}
												add_to_variable = {
													BAN_noble_control = -1
												}
												else_if = {
													limit = {
														check_variable = { BAN_bandit_control > 2 }
														check_variable = { BAN_noble_control = 0 }
													}
													add_to_variable = {
														BAN_commune_control = 3
													}
													add_to_variable = {
														BAN_bandit_control = -3
													}
													else_if = {
														limit = {
															check_variable = { BAN_bandit_control > 1 }
															check_variable = { BAN_noble_control = 0 }
														}
														add_to_variable = {
															BAN_commune_control = 2
														}
														add_to_variable = {
															BAN_bandit_control = -2
														}
														else_if = {
															limit = {
																check_variable = { BAN_bandit_control > 0 }
																check_variable = { BAN_noble_control = 0 }
															}
															add_to_variable = {
																BAN_commune_control = 1
															}
															add_to_variable = {
																BAN_bandit_control = -1
															}
															else = {
																#
															}
														}
													}
												}
											}
										}
									}
								}
							}
						}
					}
					if = {
						limit = {
							ROOT = { NOT = { has_completed_focus = BAN_no_compromise } }
						}
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 2 }
							}
							add_to_variable = {
								BAN_commune_control = 3
							}
							add_to_variable = {
								BAN_bandit_control = -3
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 1 }
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								add_to_variable = {
									BAN_bandit_control = -2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									else = {
										#
									}
								}
							}
						}
					}
					update_BAN_variables = yes
				}
			}
		}
	}
	BAN_strengthen_communes_decision_naniwich = {
		icon = griffon
		ai_will_do = {
			factor = 10
		}
		highlight_states = {
			state = 489
		}
		visible = {
			has_completed_focus = BAN_revolutionary_spirit
			owns_state = 489
		}
		available = {
			controls_state = 489
			custom_trigger_tooltip = {
				tooltip = BAN_can_strengthen_tt
				hidden_trigger = {
					OR = {
						AND = {
							ROOT = { has_completed_focus = BAN_no_compromise }
							OR = {
								489 = {
									check_variable = { BAN_noble_control > 0 }
								}
								489 = {
									check_variable = { BAN_bandit_control > 0 }
								}
							}
						}
						AND = {
							ROOT = { NOT = { has_completed_focus = BAN_no_compromise } }
							489 = {
								check_variable = { BAN_bandit_control > 0 }
							}
						}
					}
				}
			}
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		cost = 0
		days_remove = 20
		complete_effect = {
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_strengthen_communes_decision_naniwich"
			custom_effect_tooltip = BAN_add_3_commune_control_naniwich_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			hidden_effect = {
				489 = {
					if = {
						limit = {
							ROOT = { has_completed_focus = BAN_no_compromise }
						}
						if = {
							limit = {
								check_variable = { BAN_noble_control > 0 }
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_commune_control = 3
							}
							add_to_variable = {
								BAN_noble_control = -1
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_noble_control > 1 }
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_commune_control = 3
								}
								add_to_variable = {
									BAN_noble_control = -2
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
								else_if = {
									limit = {
										check_variable = { BAN_noble_control > 0 }
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_commune_control = 2
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									add_to_variable = {
										BAN_noble_control = -1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control = 0 }
											check_variable = { BAN_noble_control > 2 }
										}
										add_to_variable = {
											BAN_commune_control = 3
										}
										add_to_variable = {
											BAN_noble_control = -3
										}
										else_if = {
											limit = {
												check_variable = { BAN_bandit_control = 0 }
												check_variable = { BAN_noble_control > 1 }
											}
											add_to_variable = {
												BAN_commune_control = 2
											}
											add_to_variable = {
												BAN_noble_control = -2
											}
											else_if = {
												limit = {
													check_variable = { BAN_bandit_control = 0 }
													check_variable = { BAN_noble_control > 0 }
												}
												add_to_variable = {
													BAN_commune_control = 1
												}
												add_to_variable = {
													BAN_noble_control = -1
												}
												else_if = {
													limit = {
														check_variable = { BAN_bandit_control > 2 }
														check_variable = { BAN_noble_control = 0 }
													}
													add_to_variable = {
														BAN_commune_control = 3
													}
													add_to_variable = {
														BAN_bandit_control = -3
													}
													else_if = {
														limit = {
															check_variable = { BAN_bandit_control > 1 }
															check_variable = { BAN_noble_control = 0 }
														}
														add_to_variable = {
															BAN_commune_control = 2
														}
														add_to_variable = {
															BAN_bandit_control = -2
														}
														else_if = {
															limit = {
																check_variable = { BAN_bandit_control > 0 }
																check_variable = { BAN_noble_control = 0 }
															}
															add_to_variable = {
																BAN_commune_control = 1
															}
															add_to_variable = {
																BAN_bandit_control = -1
															}
															else = {
																#
															}
														}
													}
												}
											}
										}
									}
								}
							}
						}
					}
					if = {
						limit = {
							ROOT = { NOT = { has_completed_focus = BAN_no_compromise } }
						}
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 2 }
							}
							add_to_variable = {
								BAN_commune_control = 3
							}
							add_to_variable = {
								BAN_bandit_control = -3
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 1 }
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								add_to_variable = {
									BAN_bandit_control = -2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									else = {
										#
									}
								}
							}
						}
					}
					update_BAN_variables = yes
				}
			}
		}
	}
	BAN_strengthen_communes_decision_creeper = {
		icon = griffon
		ai_will_do = {
			factor = 10
		}
		highlight_states = {
			state = 446
		}
		visible = {
			has_completed_focus = BAN_revolutionary_spirit
			owns_state = 446
		}
		available = {
			controls_state = 446
			custom_trigger_tooltip = {
				tooltip = BAN_can_strengthen_tt
				hidden_trigger = {
					OR = {
						AND = {
							ROOT = { has_completed_focus = BAN_no_compromise }
							OR = {
								446 = {
									check_variable = { BAN_noble_control > 0 }
								}
								446 = {
									check_variable = { BAN_bandit_control > 0 }
								}
							}
						}
						AND = {
							ROOT = { NOT = { has_completed_focus = BAN_no_compromise } }
							446 = {
								check_variable = { BAN_bandit_control > 0 }
							}
						}
					}
				}
			}
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		cost = 0
		days_remove = 20
		complete_effect = {
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_strengthen_communes_decision_creeper"
			custom_effect_tooltip = BAN_add_3_commune_control_creeper_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			hidden_effect = {
				446 = {
					if = {
						limit = {
							ROOT = { has_completed_focus = BAN_no_compromise }
						}
						if = {
							limit = {
								check_variable = { BAN_noble_control > 0 }
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_commune_control = 3
							}
							add_to_variable = {
								BAN_noble_control = -1
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_noble_control > 1 }
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_commune_control = 3
								}
								add_to_variable = {
									BAN_noble_control = -2
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
								else_if = {
									limit = {
										check_variable = { BAN_noble_control > 0 }
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_commune_control = 2
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									add_to_variable = {
										BAN_noble_control = -1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control = 0 }
											check_variable = { BAN_noble_control > 2 }
										}
										add_to_variable = {
											BAN_commune_control = 3
										}
										add_to_variable = {
											BAN_noble_control = -3
										}
										else_if = {
											limit = {
												check_variable = { BAN_bandit_control = 0 }
												check_variable = { BAN_noble_control > 1 }
											}
											add_to_variable = {
												BAN_commune_control = 2
											}
											add_to_variable = {
												BAN_noble_control = -2
											}
											else_if = {
												limit = {
													check_variable = { BAN_bandit_control = 0 }
													check_variable = { BAN_noble_control > 0 }
												}
												add_to_variable = {
													BAN_commune_control = 1
												}
												add_to_variable = {
													BAN_noble_control = -1
												}
												else_if = {
													limit = {
														check_variable = { BAN_bandit_control > 2 }
														check_variable = { BAN_noble_control = 0 }
													}
													add_to_variable = {
														BAN_commune_control = 3
													}
													add_to_variable = {
														BAN_bandit_control = -3
													}
													else_if = {
														limit = {
															check_variable = { BAN_bandit_control > 1 }
															check_variable = { BAN_noble_control = 0 }
														}
														add_to_variable = {
															BAN_commune_control = 2
														}
														add_to_variable = {
															BAN_bandit_control = -2
														}
														else_if = {
															limit = {
																check_variable = { BAN_bandit_control > 0 }
																check_variable = { BAN_noble_control = 0 }
															}
															add_to_variable = {
																BAN_commune_control = 1
															}
															add_to_variable = {
																BAN_bandit_control = -1
															}
															else = {
																#
															}
														}
													}
												}
											}
										}
									}
								}
							}
						}
					}
					if = {
						limit = {
							ROOT = { NOT = { has_completed_focus = BAN_no_compromise } }
						}
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 2 }
							}
							add_to_variable = {
								BAN_commune_control = 3
							}
							add_to_variable = {
								BAN_bandit_control = -3
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 1 }
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								add_to_variable = {
									BAN_bandit_control = -2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									else = {
										#
									}
								}
							}
						}
					}
					update_BAN_variables = yes
				}
			}
		}
	}
	BAN_strengthen_communes_decision_banditplace = {
		icon = griffon
		ai_will_do = {
			factor = 10
		}
		highlight_states = {
			state = 606
		}
		visible = {
			has_completed_focus = BAN_revolutionary_spirit
			owns_state = 606
		}
		available = {
			controls_state = 606
			custom_trigger_tooltip = {
				tooltip = BAN_can_strengthen_tt
				hidden_trigger = {
					OR = {
						AND = {
							ROOT = { has_completed_focus = BAN_no_compromise }
							OR = {
								606 = {
									check_variable = { BAN_noble_control > 0 }
								}
								606 = {
									check_variable = { BAN_bandit_control > 0 }
								}
							}
						}
						AND = {
							ROOT = { NOT = { has_completed_focus = BAN_no_compromise } }
							606 = {
								check_variable = { BAN_bandit_control > 0 }
							}
						}
					}
				}
			}
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		cost = 0
		days_remove = 20
		complete_effect = {
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_strengthen_communes_decision_banditplace"
			custom_effect_tooltip = BAN_add_3_commune_control_banditplace_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			hidden_effect = {
				606 = {
					if = {
						limit = {
							ROOT = { has_completed_focus = BAN_no_compromise }
						}
						if = {
							limit = {
								check_variable = { BAN_noble_control > 0 }
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_commune_control = 3
							}
							add_to_variable = {
								BAN_noble_control = -1
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_noble_control > 1 }
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_commune_control = 3
								}
								add_to_variable = {
									BAN_noble_control = -2
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
								else_if = {
									limit = {
										check_variable = { BAN_noble_control > 0 }
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_commune_control = 2
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									add_to_variable = {
										BAN_noble_control = -1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control = 0 }
											check_variable = { BAN_noble_control > 2 }
										}
										add_to_variable = {
											BAN_commune_control = 3
										}
										add_to_variable = {
											BAN_noble_control = -3
										}
										else_if = {
											limit = {
												check_variable = { BAN_bandit_control = 0 }
												check_variable = { BAN_noble_control > 1 }
											}
											add_to_variable = {
												BAN_commune_control = 2
											}
											add_to_variable = {
												BAN_noble_control = -2
											}
											else_if = {
												limit = {
													check_variable = { BAN_bandit_control = 0 }
													check_variable = { BAN_noble_control > 0 }
												}
												add_to_variable = {
													BAN_commune_control = 1
												}
												add_to_variable = {
													BAN_noble_control = -1
												}
												else_if = {
													limit = {
														check_variable = { BAN_bandit_control > 2 }
														check_variable = { BAN_noble_control = 0 }
													}
													add_to_variable = {
														BAN_commune_control = 3
													}
													add_to_variable = {
														BAN_bandit_control = -3
													}
													else_if = {
														limit = {
															check_variable = { BAN_bandit_control > 1 }
															check_variable = { BAN_noble_control = 0 }
														}
														add_to_variable = {
															BAN_commune_control = 2
														}
														add_to_variable = {
															BAN_bandit_control = -2
														}
														else_if = {
															limit = {
																check_variable = { BAN_bandit_control > 0 }
																check_variable = { BAN_noble_control = 0 }
															}
															add_to_variable = {
																BAN_commune_control = 1
															}
															add_to_variable = {
																BAN_bandit_control = -1
															}
															else = {
																#
															}
														}
													}
												}
											}
										}
									}
								}
							}
						}
					}
					if = {
						limit = {
							ROOT = { NOT = { has_completed_focus = BAN_no_compromise } }
						}
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 2 }
							}
							add_to_variable = {
								BAN_commune_control = 3
							}
							add_to_variable = {
								BAN_bandit_control = -3
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 1 }
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								add_to_variable = {
									BAN_bandit_control = -2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									else = {
										#
									}
								}
							}
						}
					}
					update_BAN_variables = yes
				}
			}
		}
	}
	BAN_strengthen_communes_decision_blackhollow = {
		icon = griffon
		ai_will_do = {
			factor = 10
		}
		highlight_states = {
			state = 490
		}
		visible = {
			has_completed_focus = BAN_revolutionary_spirit
			owns_state = 490
		}
		available = {
			controls_state = 490
			custom_trigger_tooltip = {
				tooltip = BAN_can_strengthen_tt
				hidden_trigger = {
					OR = {
						AND = {
							ROOT = { has_completed_focus = BAN_no_compromise }
							OR = {
								490 = {
									check_variable = { BAN_noble_control > 0 }
								}
								490 = {
									check_variable = { BAN_bandit_control > 0 }
								}
							}
						}
						AND = {
							ROOT = { NOT = { has_completed_focus = BAN_no_compromise } }
							490 = {
								check_variable = { BAN_bandit_control > 0 }
							}
						}
					}
				}
			}
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		cost = 0
		days_remove = 20
		complete_effect = {
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_strengthen_communes_decision_blackhollow"
			custom_effect_tooltip = BAN_add_3_commune_control_blackhollow_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			hidden_effect = {
				490 = {
					if = {
						limit = {
							ROOT = { has_completed_focus = BAN_no_compromise }
						}
						if = {
							limit = {
								check_variable = { BAN_noble_control > 0 }
								check_variable = { BAN_bandit_control > 1 }
							}
							add_to_variable = {
								BAN_commune_control = 3
							}
							add_to_variable = {
								BAN_noble_control = -1
							}
							add_to_variable = {
								BAN_bandit_control = -2
							}
							else_if = {
								limit = {
									check_variable = { BAN_noble_control > 1 }
									check_variable = { BAN_bandit_control > 0 }
								}
								add_to_variable = {
									BAN_commune_control = 3
								}
								add_to_variable = {
									BAN_noble_control = -2
								}
								add_to_variable = {
									BAN_bandit_control = -1
								}
								else_if = {
									limit = {
										check_variable = { BAN_noble_control > 0 }
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_commune_control = 2
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									add_to_variable = {
										BAN_noble_control = -1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control = 0 }
											check_variable = { BAN_noble_control > 2 }
										}
										add_to_variable = {
											BAN_commune_control = 3
										}
										add_to_variable = {
											BAN_noble_control = -3
										}
										else_if = {
											limit = {
												check_variable = { BAN_bandit_control = 0 }
												check_variable = { BAN_noble_control > 1 }
											}
											add_to_variable = {
												BAN_commune_control = 2
											}
											add_to_variable = {
												BAN_noble_control = -2
											}
											else_if = {
												limit = {
													check_variable = { BAN_bandit_control = 0 }
													check_variable = { BAN_noble_control > 0 }
												}
												add_to_variable = {
													BAN_commune_control = 1
												}
												add_to_variable = {
													BAN_noble_control = -1
												}
												else_if = {
													limit = {
														check_variable = { BAN_bandit_control > 2 }
														check_variable = { BAN_noble_control = 0 }
													}
													add_to_variable = {
														BAN_commune_control = 3
													}
													add_to_variable = {
														BAN_bandit_control = -3
													}
													else_if = {
														limit = {
															check_variable = { BAN_bandit_control > 1 }
															check_variable = { BAN_noble_control = 0 }
														}
														add_to_variable = {
															BAN_commune_control = 2
														}
														add_to_variable = {
															BAN_bandit_control = -2
														}
														else_if = {
															limit = {
																check_variable = { BAN_bandit_control > 0 }
																check_variable = { BAN_noble_control = 0 }
															}
															add_to_variable = {
																BAN_commune_control = 1
															}
															add_to_variable = {
																BAN_bandit_control = -1
															}
															else = {
																#
															}
														}
													}
												}
											}
										}
									}
								}
							}
						}
					}
					if = {
						limit = {
							ROOT = { NOT = { has_completed_focus = BAN_no_compromise } }
						}
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 2 }
							}
							add_to_variable = {
								BAN_commune_control = 3
							}
							add_to_variable = {
								BAN_bandit_control = -3
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 1 }
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								add_to_variable = {
									BAN_bandit_control = -2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 0 }
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = -1
									}
									else = {
										#
									}
								}
							}
						}
					}
					update_BAN_variables = yes
				}
			}
		}
	}
	BAN_recruit_commune_division_decision = {
		icon = rifles
		visible = {
			has_completed_focus = BAN_revolutionary_spirit
		}
		available = {
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		days_remove = 30
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BAN_recruit_commune_division"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_recruit_commune_division"
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			custom_effect_tooltip = BAN_recruit_commune_division
			add_to_variable = { BAN_commune_divisions = 1 }
		}
	}

	BAN_speak_with_pinfeather = {
		icon = griffon
		fire_only_once = yes
		visible = {
			has_completed_focus = BAN_sway_generals
			NOT = {
				has_country_flag = BAN_pinfeather_killed
			}
		}
		available = {
			NOT = {
				has_country_flag = BAN_two_decisions_running
			}
		}
		days_remove = 10
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_speak_with_pinfeather"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_speak_with_pinfeather"
			country_event = { id = bandit.40 }
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
		}
	}
	BAN_align_pinfeather = {
		icon = griffon
		fire_only_once = yes
		visible = {
			has_country_flag = BAN_pinfeather_is_spoken_to
			NOT = {
				has_country_flag = BAN_pinfeather_killed
			}
		}
		available = {
			NOT = {
				has_country_flag = BAN_two_decisions_running
			}
		}
		days_remove = 10
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_align_pinfeather"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_align_pinfeather"
			country_event = { id = bandit.41 }
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
		}
	}
	BAN_get_report_from_pinfeather = {
		icon = griffon
		visible = {
			OR = {
				AND = {
					has_completed_focus = BAN_noble_spirit
					has_country_flag = BAN_cyrille_pinfeather
				}
				AND = {
					has_completed_focus = BAN_revolutionary_spirit
					has_country_flag = BAN_reeve_pinfeather
				}
			}
		}
		available = {
			NOT = {
				has_country_flag = BAN_pinfeather_killed
			}
		}
		ai_will_do = {
			factor = 0
		}
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_get_report_from_pinfeather"
			set_country_flag = BAN_use_pinfeather_desc
			clr_country_flag = BAN_use_bribe_desc
			country_event = { id = bandit.42 }
		}
	}

	BAN_bribe_bandit_govt = {
		icon = griffon
		visible = {
			has_completed_focus = BAN_bribe_bandits
		}
		available = {
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		ai_will_do = {
			factor = 5
		}
		days_remove = 20
		fire_only_once = yes
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_bribe_bandit_govt"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_bribe_bandit_govt"
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_bribed_govt_flag
			custom_effect_tooltip = BAN_remove_1_bandit_control_every_state_tt
			hidden_effect = {
				every_owned_state = {
					limit = {
						OR = {
							state = 446
							state = 606
							state = 489
							state = 490
							state = 532
						}
					}
					if = {
						limit = { check_variable = { BAN_bandit_control > 0 } }
						if = {
							limit = {
								ROOT = { has_completed_focus = BAN_noble_spirit }
							}
							add_to_variable = {
								BAN_noble_control = 1
							}
						}
						if = {
							limit = {
								ROOT = { has_completed_focus = BAN_revolutionary_spirit }
							}
							add_to_variable = {
								BAN_commune_control = 1
							}
						}
						add_to_variable = {
							BAN_bandit_control = -1
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_use_contacts = {
		icon = griffon
		visible = {
			has_country_flag = BAN_bribed_govt_flag
		}
		available = {
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		ai_will_do = {
			factor = 5
		}
		days_remove = 10
		days_re_enable = 30
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_bribe_bandit_govt"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_bribe_bandit_govt"
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_use_bribe_desc
			clr_country_flag = BAN_use_pinfeather_desc
			country_event = {
				id = bandit.42
			}
			custom_effect_tooltip = BAN_remove_1_bandit_control_every_state_tt
			hidden_effect = {
				every_owned_state = {
					limit = {
						OR = {
							state = 446
							state = 606
							state = 489
							state = 490
							state = 532
						}
					}
					if = {
						limit = { check_variable = { BAN_bandit_control > 0 } }
						if = {
							limit = {
								ROOT = { has_completed_focus = BAN_noble_spirit }
							}
							add_to_variable = {
								BAN_noble_control = 1
							}
						}
						if = {
							limit = {
								ROOT = { has_completed_focus = BAN_revolutionary_spirit }
							}
							add_to_variable = {
								BAN_commune_control = 1
							}
						}
						add_to_variable = {
							BAN_bandit_control = -1
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}

	BAN_kill_pinfeather = {
		icon = griffon
		visible = {
			has_completed_focus = BAN_assassinate_bandits
			NOT = {
				has_country_flag = BAN_pinfeather_killed
			}
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = BAN_can_kill_pinfeather_tt
				NOT = {
					AND = {
						OR = {
							has_completed_focus = BAN_noble_spirit
							has_completed_focus = BAN_united_resistance_reeve
						}
						has_country_flag = BAN_cyrille_pinfeather
					}
					AND = {
							OR = {
							has_completed_focus = BAN_revolutionary_spirit
							has_completed_focus = BAN_united_resistance_cyrille
						}	
						has_country_flag = BAN_reeve_pinfeather
					}
				}
			}
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		days_remove = 20
		fire_only_once = yes
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_kill_pinfeather"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_kill_pinfeather"
			remove_unit_leader = 69001
			custom_effect_tooltip = BAN_assassination_effect_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_pinfeather_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_dominik_controls_state
						}
						clr_state_flag = BAN_dominik_controls_state
						set_state_flag = BAN_blackrock_controls_state
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 4 }
							}
							add_to_variable = {
								BAN_bandit_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 3 }
								}
								add_to_variable = {
									BAN_bandit_control = -4
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 2 }
									}
									add_to_variable = {
										BAN_bandit_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control > 1 }
										}
										add_to_variable = {
											BAN_bandit_control = -2
										}
										add_to_variable = {
											BAN_noble_control = 1
										}
										add_to_variable = {
											BAN_commune_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_bandit_control > 0 }
											}
											add_to_variable = {
												BAN_bandit_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_kill_hiperion = {
		icon = griffon
		visible = {
			has_completed_focus = BAN_assassinate_bandits
			NOT = {
				has_country_flag = BAN_hiperion_killed
			}
		}
		available = {
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		days_remove = 20
		fire_only_once = yes
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_kill_hiperion"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_kill_hiperion"
			remove_unit_leader = 69002
			custom_effect_tooltip = BAN_assassination_effect_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_hiperion_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_ulrich_controls_state
						}
						clr_state_flag = BAN_ulrich_controls_state
						set_state_flag = BAN_blackrock_controls_state
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 4 }
							}
							add_to_variable = {
								BAN_bandit_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 3 }
								}
								add_to_variable = {
									BAN_bandit_control = -4
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 2 }
									}
									add_to_variable = {
										BAN_bandit_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control > 1 }
										}
										add_to_variable = {
											BAN_bandit_control = -2
										}
										add_to_variable = {
											BAN_noble_control = 1
										}
										add_to_variable = {
											BAN_commune_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_bandit_control > 0 }
											}
											add_to_variable = {
												BAN_bandit_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_kill_bronsfeather = {
		icon = griffon
		visible = {
			has_completed_focus = BAN_assassinate_bandits
			NOT = {
				has_country_flag = BAN_bronsfeather_killed
			}
		}
		available = {
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		days_remove = 20
		fire_only_once = yes
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_kill_bronsfeather"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_kill_bronsfeather"
			remove_unit_leader = 69000
			custom_effect_tooltip = BAN_assassination_effect_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_bronsfeather_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_kajetan_controls_state
						}
						clr_state_flag = BAN_kajetan_controls_state
						set_state_flag = BAN_blackrock_controls_state
						if = {
							limit = {
								check_variable = { BAN_bandit_control > 4 }
							}
							add_to_variable = {
								BAN_bandit_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_bandit_control > 3 }
								}
								add_to_variable = {
									BAN_bandit_control = -4
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_bandit_control > 2 }
									}
									add_to_variable = {
										BAN_bandit_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_bandit_control > 1 }
										}
										add_to_variable = {
											BAN_bandit_control = -2
										}
										add_to_variable = {
											BAN_noble_control = 1
										}
										add_to_variable = {
											BAN_commune_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_bandit_control > 0 }
											}
											add_to_variable = {
												BAN_bandit_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_kill_blackbeak = {
		icon = griffon
		visible = {
			has_completed_focus = BAN_target_nobles
			NOT = {
				has_country_flag = BAN_blackbeak_killed
			}
		}
		available = {
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		days_remove = 20
		fire_only_once = yes
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_kill_blackbeak"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_kill_blackbeak"
			custom_effect_tooltip = BAN_assassination_effect_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_blackbeak_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_silas_controls_state
						}
						clr_state_flag = BAN_silas_controls_state
						set_state_flag = BAN_cyrille_controls_state
						if = {
							limit = {
								check_variable = { BAN_noble_control > 4 }
							}
							add_to_variable = {
								BAN_noble_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_bandit_control = 2
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_noble_control > 3 }
								}
								add_to_variable = {
									BAN_noble_control = -4
								}
								add_to_variable = {
									BAN_bandit_control = 2
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_noble_control > 2 }
									}
									add_to_variable = {
										BAN_noble_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = 1
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_noble_control > 1 }
										}
										add_to_variable = {
											BAN_noble_control = -2
										}
										add_to_variable = {
											BAN_bandit_control = 1
										}
										add_to_variable = {
											BAN_commune_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_noble_control > 0 }
											}
											add_to_variable = {
												BAN_noble_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_kill_sir = {
		icon = griffon
		visible = {
			has_completed_focus = BAN_target_nobles
			has_country_flag = BAN_sir_available
			NOT = {
				has_country_flag = BAN_sir_killed
			}
		}
		available = {
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		days_remove = 20
		fire_only_once = yes
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_kill_sir"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_kill_sir"
			custom_effect_tooltip = BAN_assassination_effect_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_sir_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_sir_controls_state
						}
						clr_state_flag = BAN_sir_controls_state
						set_state_flag = BAN_cyrille_controls_state
						if = {
							limit = {
								check_variable = { BAN_noble_control > 4 }
							}
							add_to_variable = {
								BAN_noble_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_bandit_control = 2
							}
							add_to_variable = {
								BAN_commune_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_noble_control > 3 }
								}
								add_to_variable = {
									BAN_noble_control = -4
								}
								add_to_variable = {
									BAN_bandit_control = 2
								}
								add_to_variable = {
									BAN_commune_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_noble_control > 2 }
									}
									add_to_variable = {
										BAN_noble_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = 1
									}
									add_to_variable = {
										BAN_commune_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_noble_control > 1 }
										}
										add_to_variable = {
											BAN_noble_control = -2
										}
										add_to_variable = {
											BAN_bandit_control = 1
										}
										add_to_variable = {
											BAN_commune_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_noble_control > 0 }
											}
											add_to_variable = {
												BAN_noble_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_kill_tercel = {
		icon = griffon
		visible = {
			has_completed_focus = BAN_target_communists
			NOT = {
				has_country_flag = BAN_tercel_killed
			}
		}
		available = {
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		days_remove = 20
		fire_only_once = yes
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_kill_tercel"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_kill_tercel"
			custom_effect_tooltip = BAN_assassination_effect_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_tercel_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_gavrila_controls_state
						}
						clr_state_flag = BAN_gavrila_controls_state
						set_state_flag = BAN_reeve_controls_state
						if = {
							limit = {
								check_variable = { BAN_commune_control > 4 }
							}
							add_to_variable = {
								BAN_commune_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_bandit_control = 2
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_commune_control > 3 }
								}
								add_to_variable = {
									BAN_commune_control = -4
								}
								add_to_variable = {
									BAN_bandit_control = 2
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_commune_control > 2 }
									}
									add_to_variable = {
										BAN_commune_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = 1
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_commune_control > 1 }
										}
										add_to_variable = {
											BAN_commune_control = -2
										}
										add_to_variable = {
											BAN_bandit_control = 1
										}
										add_to_variable = {
											BAN_noble_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_commune_control > 0 }
											}
											add_to_variable = {
												BAN_commune_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}
	BAN_kill_horndrogiannis = {
		icon = griffon
		visible = {
			has_completed_focus = BAN_target_communists
			NOT = {
				has_country_flag = BAN_horndrogiannis_killed
			}
		}
		available = {
			NOT = { has_country_flag = BAN_two_decisions_running }
			hidden_trigger = {
				NOT = { has_country_flag = BAN_cant_do_resistance }
			}
		}
		days_remove = 20
		fire_only_once = yes
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_kill_horndrogiannis"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_kill_horndrogiannis"
			custom_effect_tooltip = BAN_assassination_effect_tt
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			set_country_flag = BAN_horndrogiannis_killed
			hidden_effect = {
				every_owned_state = {
					if = {
						limit = {
							has_state_flag = BAN_giannis_controls_state
						}
						clr_state_flag = BAN_giannis_controls_state
						set_state_flag = BAN_reeve_controls_state
						if = {
							limit = {
								check_variable = { BAN_commune_control > 4 }
							}
							add_to_variable = {
								BAN_commune_control = -5
							}
							add_to_variable = {
								BAN_religious_control = 1
							}
							add_to_variable = {
								BAN_bandit_control = 2
							}
							add_to_variable = {
								BAN_noble_control = 2
							}
							else_if = {
								limit = {
									check_variable = { BAN_commune_control > 3 }
								}
								add_to_variable = {
									BAN_commune_control = -4
								}
								add_to_variable = {
									BAN_bandit_control = 2
								}
								add_to_variable = {
									BAN_noble_control = 2
								}
								else_if = {
									limit = {
										check_variable = { BAN_commune_control > 2 }
									}
									add_to_variable = {
										BAN_commune_control = -3
									}
									add_to_variable = {
										BAN_religious_control = 1
									}
									add_to_variable = {
										BAN_bandit_control = 1
									}
									add_to_variable = {
										BAN_noble_control = 1
									}
									else_if = {
										limit = {
											check_variable = { BAN_commune_control > 1 }
										}
										add_to_variable = {
											BAN_commune_control = -2
										}
										add_to_variable = {
											BAN_bandit_control = 1
										}
										add_to_variable = {
											BAN_noble_control = 1
										}
										else_if = {
											limit = {
												check_variable = { BAN_commune_control > 0 }
											}
											add_to_variable = {
												BAN_commune_control = -1
											}
											add_to_variable = {
												BAN_religious_control = 1
											}
											else = {
												#
											}
										}
									}
								}
							}
						}
					}
				}
				update_BAN_variables = yes
			}
		}
	}

	BAN_fight_for_blackhollow = {
		icon = rifles
		visible = {
			owns_state = 490
			OR = {
				has_completed_focus = BAN_noble_spirit
				has_completed_focus = BAN_revolutionary_spirit
			}
		}
		
		available = {
			custom_trigger_tooltip = {
				tooltip = BAN_has_bigger_faction_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								490 = { has_state_flag = BAN_actual_controller_nobles }
								AND = {
									has_completed_focus = BAN_oppose_communism
									OR = {
										AND = {
											490 = { has_state_flag = BAN_actual_controller_bandits }
											490 = { check_variable = { BAN_noble_control > BAN_bandit_control } }
										}
										AND = {
											490 = { has_state_flag = BAN_actual_controller_communes }
											490 = { check_variable = { BAN_noble_control > BAN_commune_control } }
										}
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_oppose_communism }
									490 = { has_state_flag = BAN_actual_controller_bandits }
									490 = { check_variable = { BAN_noble_control > BAN_bandit_control } }
								}
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								490 = { has_state_flag = BAN_actual_controllers_communes }
								AND = {
									has_completed_focus = BAN_no_compromise
									OR = {
										AND = {
											490 = { has_state_flag = BAN_actual_controller_bandits }
											490 = { check_variable = { BAN_commune_control > BAN_bandit_control } }
										}
										AND = {
											490 = { has_state_flag = BAN_actual_controller_nobles }
											490 = { check_variable = { BAN_commune_control > BAN_noble_control } }
										}
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_no_compromise }
									490 = { has_state_flag = BAN_actual_controller_bandits }
									490 = { check_variable = { BAN_commune_control > BAN_bandit_control } }
								}
							}
						}
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = BAN_can_battle_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								check_variable = { BAN_available_noble_divisions > 0 }
								check_variable = { BAN_available_special_noble_divisions > 0 }
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								check_variable = { BAN_available_commune_divisions > 0 }
								check_variable = { BAN_available_special_commune_divisions > 0 }
							}
						}
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = BAN_unfriendly_state_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								AND = {
									has_completed_focus = BAN_oppose_communism
									OR = {
										490 = { has_state_flag = BAN_actual_controller_bandits }
										490 = { has_state_flag = BAN_actual_controller_communes }
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_oppose_communism }
									490 = { has_state_flag = BAN_actual_controller_bandits }
								}
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								AND = {
									has_completed_focus = BAN_no_compromise
									OR = {
										490 = { has_state_flag = BAN_actual_controller_bandits }
										490 = { has_state_flag = BAN_actual_controller_nobles }
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_no_compromise }
									490 = { has_state_flag = BAN_actual_controller_bandits }
								}
							}
						}
					}
				}
			}
			NOT = {
				has_country_flag = BAN_two_decisions_running
				has_country_flag = BAN_battle_planned
			}
		}
		highlight_states = {
			state = 490
		}
		days_remove = 20

		modifier = {
			political_power_gain = -0.50
			stability_factor = -0.15
		}

		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_fight_for_blackhollow"
			490 = { save_global_event_target_as = BAN_battle_target }
			set_country_flag = BAN_battle_planned
			country_event = { id = bandit.37 }
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_fight_for_blackhollow"
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
			clr_country_flag = BAN_battle_planned
			custom_effect_tooltip = BAN_battle_happen_tt
			hidden_effect = {
				country_event = {
					id = bandit.29
				}
			}
		}
		ai_will_do = {
			factor = 400
		}
	}
	BAN_fight_for_murkbeak = {
		icon = rifles
		visible = {
			owns_state = 532
			OR = {
				has_completed_focus = BAN_noble_spirit
				has_completed_focus = BAN_revolutionary_spirit
			}
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = BAN_has_bigger_faction_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								532 = { has_state_flag = BAN_actual_controller_nobles }
								AND = {
									has_completed_focus = BAN_oppose_communism
									OR = {
										AND = {
											532 = { has_state_flag = BAN_actual_controller_bandits }
											532 = { check_variable = { BAN_noble_control > BAN_bandit_control } }
										}
										AND = {
											532 = { has_state_flag = BAN_actual_controller_communes }
											532 = { check_variable = { BAN_noble_control > BAN_commune_control } }
										}
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_oppose_communism }
									532 = { has_state_flag = BAN_actual_controller_bandits }
									532 = { check_variable = { BAN_noble_control > BAN_bandit_control } }
								}
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								532 = { has_state_flag = BAN_actual_controllers_communes }
								AND = {
									has_completed_focus = BAN_no_compromise
									OR = {
										AND = {
											532 = { has_state_flag = BAN_actual_controller_bandits }
											532 = { check_variable = { BAN_commune_control > BAN_bandit_control } }
										}
										AND = {
											532 = { has_state_flag = BAN_actual_controller_nobles }
											532 = { check_variable = { BAN_commune_control > BAN_noble_control } }
										}
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_no_compromise }
									532 = { has_state_flag = BAN_actual_controller_bandits }
									532 = { check_variable = { BAN_commune_control > BAN_bandit_control } }
								}
							}
						}
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = BAN_can_battle_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								check_variable = { BAN_available_noble_divisions > 0 }
								check_variable = { BAN_available_special_noble_divisions > 0 }
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								check_variable = { BAN_available_commune_divisions > 0 }
								check_variable = { BAN_available_special_commune_divisions > 0 }
							}
						}
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = BAN_unfriendly_state_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								AND = {
									has_completed_focus = BAN_oppose_communism
									OR = {
										532 = { has_state_flag = BAN_actual_controller_bandits }
										532 = { has_state_flag = BAN_actual_controller_communes }
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_oppose_communism }
									532 = { has_state_flag = BAN_actual_controller_bandits }
								}
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								AND = {
									has_completed_focus = BAN_no_compromise
									OR = {
										532 = { has_state_flag = BAN_actual_controller_bandits }
										532 = { has_state_flag = BAN_actual_controller_nobles }
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_no_compromise }
									532 = { has_state_flag = BAN_actual_controller_bandits }
								}
							}
						}
					}
				}
			}
			NOT = {
				has_country_flag = BAN_two_decisions_running
				has_country_flag = BAN_battle_planned
			}
		}
		highlight_states = {
			state = 532
		}
		days_remove = 20
		fire_only_once = yes

		modifier = {
			political_power_gain = -0.50
			stability_factor = -0.15

		}

		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_fight_for_murkbeak"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
			532 = { save_global_event_target_as = BAN_battle_target }
			set_country_flag = BAN_battle_planned
			country_event = { id = bandit.37 }
		}

		ai_will_do = {
			factor = 400
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_fight_for_murkbeak"
			clr_country_flag = BAN_battle_planned
			custom_effect_tooltip = BAN_battle_happen_tt
			hidden_effect = {
				country_event = {
					id = bandit.30
				}
			}
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
		}
	}
	BAN_fight_for_creeper_mountains = {
		icon = rifles
		visible = {
			owns_state = 446
			OR = {
				has_completed_focus = BAN_noble_spirit
				has_completed_focus = BAN_revolutionary_spirit
			}
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = BAN_has_bigger_faction_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								446 = { has_state_flag = BAN_actual_controller_nobles }
								AND = {
									has_completed_focus = BAN_oppose_communism
									OR = {
										AND = {
											446 = { has_state_flag = BAN_actual_controller_bandits }
											446 = { check_variable = { BAN_noble_control > BAN_bandit_control } }
										}
										AND = {
											446 = { has_state_flag = BAN_actual_controller_communes }
											446 = { check_variable = { BAN_noble_control > BAN_commune_control } }
										}
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_oppose_communism }
									446 = { has_state_flag = BAN_actual_controller_bandits }
									446 = { check_variable = { BAN_noble_control > BAN_bandit_control } }
								}
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								446 = { has_state_flag = BAN_actual_controllers_communes }
								AND = {
									has_completed_focus = BAN_no_compromise
									OR = {
										AND = {
											446 = { has_state_flag = BAN_actual_controller_bandits }
											446 = { check_variable = { BAN_commune_control > BAN_bandit_control } }
										}
										AND = {
											446 = { has_state_flag = BAN_actual_controller_nobles }
											446 = { check_variable = { BAN_commune_control > BAN_noble_control } }
										}
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_no_compromise }
									446 = { has_state_flag = BAN_actual_controller_bandits }
									446 = { check_variable = { BAN_commune_control > BAN_bandit_control } }
								}
							}
						}
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = BAN_can_battle_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								check_variable = { BAN_available_noble_divisions > 0 }
								check_variable = { BAN_available_special_noble_divisions > 0 }
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								check_variable = { BAN_available_commune_divisions > 0 }
								check_variable = { BAN_available_special_commune_divisions > 0 }
							}
						}
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = BAN_unfriendly_state_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								AND = {
									has_completed_focus = BAN_oppose_communism
									OR = {
										446 = { has_state_flag = BAN_actual_controller_bandits }
										446 = { has_state_flag = BAN_actual_controller_communes }
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_oppose_communism }
									446 = { has_state_flag = BAN_actual_controller_bandits }
								}
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								AND = {
									has_completed_focus = BAN_no_compromise
									OR = {
										446 = { has_state_flag = BAN_actual_controller_bandits }
										446 = { has_state_flag = BAN_actual_controller_nobles }
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_no_compromise }
									446 = { has_state_flag = BAN_actual_controller_bandits }
								}
							}
						}
					}
				}
			}
			NOT = {
				has_country_flag = BAN_two_decisions_running
				has_country_flag = BAN_battle_planned
			}
		}
		highlight_states = {
			state = 446
		}
		days_remove = 20
		fire_only_once = yes

		modifier = {
			political_power_gain = -0.40
			stability_factor = -0.10

		}

		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_fight_for_creeper_mountains"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
			446 = { save_global_event_target_as = BAN_battle_target }
			set_country_flag = BAN_battle_planned
			country_event = { id = bandit.37 }
		}

		ai_will_do = {
			factor = 400
		}


		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_fight_for_creeper_mountains"
			clr_country_flag = BAN_battle_planned
			custom_effect_tooltip = BAN_battle_happen_tt
			hidden_effect = {
				country_event = {
					id = bandit.31
				}
			}
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
		}
	}
	BAN_fight_for_banditplace = {
		icon = rifles
		visible = {
			owns_state = 606
			OR = {
				has_completed_focus = BAN_noble_spirit
				has_completed_focus = BAN_revolutionary_spirit
			}
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = BAN_has_bigger_faction_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								606 = { has_state_flag = BAN_actual_controller_nobles }
								AND = {
									has_completed_focus = BAN_oppose_communism
									OR = {
										AND = {
											606 = { has_state_flag = BAN_actual_controller_bandits }
											606 = { check_variable = { BAN_noble_control > BAN_bandit_control } }
										}
										AND = {
											606 = { has_state_flag = BAN_actual_controller_communes }
											606 = { check_variable = { BAN_noble_control > BAN_commune_control } }
										}
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_oppose_communism }
									606 = { has_state_flag = BAN_actual_controller_bandits }
									606 = { check_variable = { BAN_noble_control > BAN_bandit_control } }
								}
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								606 = { has_state_flag = BAN_actual_controllers_communes }
								AND = {
									has_completed_focus = BAN_no_compromise
									OR = {
										AND = {
											606 = { has_state_flag = BAN_actual_controller_bandits }
											606 = { check_variable = { BAN_commune_control > BAN_bandit_control } }
										}
										AND = {
											606 = { has_state_flag = BAN_actual_controller_nobles }
											606 = { check_variable = { BAN_commune_control > BAN_noble_control } }
										}
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_no_compromise }
									606 = { has_state_flag = BAN_actual_controller_bandits }
									606 = { check_variable = { BAN_commune_control > BAN_bandit_control } }
								}
							}
						}
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = BAN_can_battle_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								check_variable = { BAN_available_noble_divisions > 0 }
								check_variable = { BAN_available_special_noble_divisions > 0 }
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								check_variable = { BAN_available_commune_divisions > 0 }
								check_variable = { BAN_available_special_commune_divisions > 0 }
							}
						}
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = BAN_unfriendly_state_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								AND = {
									has_completed_focus = BAN_oppose_communism
									OR = {
										606 = { has_state_flag = BAN_actual_controller_bandits }
										606 = { has_state_flag = BAN_actual_controller_communes }
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_oppose_communism }
									606 = { has_state_flag = BAN_actual_controller_bandits }
								}
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								AND = {
									has_completed_focus = BAN_no_compromise
									OR = {
										606 = { has_state_flag = BAN_actual_controller_bandits }
										606 = { has_state_flag = BAN_actual_controller_nobles }
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_no_compromise }
									606 = { has_state_flag = BAN_actual_controller_bandits }
								}
							}
						}
					}
				}
			}
			NOT = {
				has_country_flag = BAN_two_decisions_running
				has_country_flag = BAN_battle_planned
			}
		}
		highlight_states = {
			state = 606
		}
		days_remove = 20
		fire_only_once = yes

		modifier = {
			political_power_gain = -0.40
			stability_factor = -0.10

		}

		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_fight_for_banditplace"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
			606 = { save_global_event_target_as = BAN_battle_target }
			set_country_flag = BAN_battle_planned
			country_event = { id = bandit.37 }
		}

		ai_will_do = {
			factor = 400
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_fight_for_banditplace"
			clr_country_flag = BAN_battle_planned
			custom_effect_tooltip = BAN_battle_happen_tt
			hidden_effect = {
				country_event = {
					id = bandit.32
				}
			}
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
		}
	}
	BAN_fight_for_naniwich = {
		icon = rifles
		visible = {
			owns_state = 489
			OR = {
				has_completed_focus = BAN_noble_spirit
				has_completed_focus = BAN_revolutionary_spirit
			}
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = BAN_has_bigger_faction_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								489 = { has_state_flag = BAN_actual_controller_nobles }
								AND = {
									has_completed_focus = BAN_oppose_communism
									OR = {
										AND = {
											489 = { has_state_flag = BAN_actual_controller_bandits }
											489 = { check_variable = { BAN_noble_control > BAN_bandit_control } }
										}
										AND = {
											489 = { has_state_flag = BAN_actual_controller_communes }
											489 = { check_variable = { BAN_noble_control > BAN_commune_control } }
										}
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_oppose_communism }
									489 = { has_state_flag = BAN_actual_controller_bandits }
									489 = { check_variable = { BAN_noble_control > BAN_bandit_control } }
								}
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								489 = { has_state_flag = BAN_actual_controllers_communes }
								AND = {
									has_completed_focus = BAN_no_compromise
									OR = {
										AND = {
											489 = { has_state_flag = BAN_actual_controller_bandits }
											489 = { check_variable = { BAN_commune_control > BAN_bandit_control } }
										}
										AND = {
											489 = { has_state_flag = BAN_actual_controller_nobles }
											489 = { check_variable = { BAN_commune_control > BAN_noble_control } }
										}
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_no_compromise }
									489 = { has_state_flag = BAN_actual_controller_bandits }
									489 = { check_variable = { BAN_commune_control > BAN_bandit_control } }
								}
							}
						}
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = BAN_can_battle_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								check_variable = { BAN_available_noble_divisions > 0 }
								check_variable = { BAN_available_special_noble_divisions > 0 }
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								check_variable = { BAN_available_commune_divisions > 0 }
								check_variable = { BAN_available_special_commune_divisions > 0 }
							}
						}
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = BAN_unfriendly_state_tt
				hidden_trigger = {
					OR = {
						AND = {
							has_completed_focus = BAN_noble_spirit
							OR = {
								AND = {
									has_completed_focus = BAN_oppose_communism
									OR = {
										489 = { has_state_flag = BAN_actual_controller_bandits }
										489 = { has_state_flag = BAN_actual_controller_communes }
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_oppose_communism }
									489 = { has_state_flag = BAN_actual_controller_bandits }
								}
							}
						}
						AND = {
							has_completed_focus = BAN_revolutionary_spirit
							OR = {
								AND = {
									has_completed_focus = BAN_no_compromise
									OR = {
										489 = { has_state_flag = BAN_actual_controller_bandits }
										489 = { has_state_flag = BAN_actual_controller_nobles }
									}
								}
								AND = {
									NOT = { has_completed_focus = BAN_no_compromise }
									489 = { has_state_flag = BAN_actual_controller_bandits }
								}
							}
						}
					}
				}
			}
			NOT = {
				has_country_flag = BAN_two_decisions_running
				has_country_flag = BAN_battle_planned
			}
		}
		highlight_states = {
			state = 489
		}
		days_remove = 20
		fire_only_once = yes

		modifier = {
			political_power_gain = -0.40
			stability_factor = -0.10

		}
		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_fight_for_naniwich"
			if = {
				limit = {
					has_country_flag = BAN_one_decision_running
				}
				set_country_flag = BAN_two_decisions_running
				else = {
					set_country_flag = BAN_one_decision_running
				}
			}
			489 = { save_global_event_target_as = BAN_battle_target }
			set_country_flag = BAN_battle_planned
			country_event = { id = bandit.37 }
		}

		ai_will_do = {
			factor = 400
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_fight_for_naniwich"
			clr_country_flag = BAN_battle_planned
			custom_effect_tooltip = BAN_battle_happen_tt
			hidden_effect = {
				country_event = {
					id = bandit.33
				}
			}
			if = {
				limit = {
					has_country_flag = BAN_two_decisions_running
				}
				clr_country_flag = BAN_two_decisions_running
				else = {
					clr_country_flag = BAN_one_decision_running
				}
			}
		}
	}
}

BAN_bandit_caches = {
	BAN_search_for_cache = {
		icon = griffon
		visible = {
			NOT = {
				has_country_flag = BAN_ongoing_search
				has_country_flag = BAN_choosing_what_to_take
			}
			owns_state = 489
			owns_state = 606
			owns_state = 446

			AND = {
				check_variable  = {
					var = cache
					value = 0
					compare = greater_than
				}
			}
		}
		allowed = {
			original_tag = BAN
		}
		days_remove = 40
		fire_only_once = no

		modifier = {
			political_power_gain = -0.5
		}

		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_search_for_cache"
			set_country_flag = BAN_ongoing_search
		}


		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAN_search_for_cache"
			activate_targeted_decision = { target = FROM decision = BAN_open_cache }
		}
		ai_will_do = {
			factor = 20
		}
	}

	BAN_open_cache = {
		icon = griffon
		visible = {
			has_country_flag = BAN_ongoing_search
		}
		available = {
			check_variable = {
				var = cache
				value = 0
				compare = greater_than
			}
		}
		allowed = { always = no }
		fire_only_once = yes
		selectable_mission = yes
		days_mission_timeout = 30
		days_remove = 10

		modifier = {
			political_power_gain = -0.5
		}

		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_open_cache"
			hidden_effect = {
				clr_country_flag = BAN_ongoing_search
				set_country_flag = BAN_choosing_what_to_take
			}


		}
		ai_will_do = {
			factor = 20
		}
	}

	BAN_take_guns = {
		icon = griffon
		allowed = {
			original_tag = BAN
		}
		visible = {
			has_country_flag = BAN_choosing_what_to_take
		}
		available = {
			check_variable  = {
				var = cache
				value = 0
				compare = greater_than
			}
		}

		fire_only_once = no

		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_take_guns"
			subtract_from_variable = {
				var = cache
				value = 1
			}
			clr_country_flag = BAN_choosing_what_to_take
			add_equipment_to_stockpile = { type = infantry_equipment_0 amount = 1000 producer = BAN }
		}
		ai_will_do = {
			factor = 20
		}
	}
	BAN_sell_loot = {
		icon = griffon
		visible = {
			original_tag = BAN
			has_country_flag = BAN_choosing_what_to_take
		}
		available = {
			original_tag = BAN
			has_country_flag = BAN_choosing_what_to_take
			check_variable  = {
				var = cache
				value = 0
				compare = greater_than
			}
		}

		fire_only_once = no

		complete_effect = {
			set_country_flag = {
				flag = BAN_cant_do_resistance
				days = 1
			}
			log = "[GetDateText]: [Root.GetName]: Decision BAN_sell_loot"
			subtract_from_variable = {
				var = cache
				value = 1
			}
			clr_country_flag = BAN_choosing_what_to_take
			add_political_power = 100
		}
		ai_will_do = {
			factor = 20
		}
	}
}
